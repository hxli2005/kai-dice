# SYNC — 双 Session 信箱

> 协议见 CLAUDE.md。编码 session 写「待决问题」与变更日志；设计 session 写「已决裁决」与 DESIGN.md。
> **冷库**：2026-08-13 起，已执行完的裁决全文、已裁待决原文存查、已完工施工单与旧变更日志整体移入 [SYNC-ARCHIVE.md](SYNC-ARCHIVE.md)——**仅史料，不得据以施工**。本文件只保留现阶段任务与规划。

## 待决问题（编码 → 设计）

<!-- 格式：
- [ ] Qn (日期, 阶段): 问题一句话。DESIGN 未覆盖处：§x。当前占位方案：…。影响面：…
-->

> **号段纪律（2026-08-13 更新）**：曾约定编码侧待决从 Q80 起编以隔开号段（2026-08-09），但设计侧编号已推进至 Q97，隔离失效并造成第三次撞号——原编码侧 Q96「掐收割均衡」按先例本区让号，改编 **Q98**（前两轮撞号更正原文见冷库）。此后**单一号段**：任一侧开新号前，检索 SYNC.md＋SYNC-ARCHIVE.md 取最大号＋1。当前最大 **Q103**，下一个可用 **Q104**。

- [ ] **Q98**（原编 Q96，2026-08-12，掐实验臂）：**掐在 1v1 残局造出「收割均衡」，要不要修？** 首批 bo5 实测（v4-pro×luna，8 场 15 手掐）抓到闭环：1 骰方看骰后被迫报价，报价即自曝（报 2 个 X ⇒ 手里必是 X 或万能 1）；对手若持同点或万能，「恰好」近乎必中。v4-pro 发现后连三局「宣抬×2 → 掐」（seed1000 第 9–11 局：回骰 1→4、抽血 99→111），**被掐者不掉骰 ⇒ 弱侧永远出不了局，任人反复收割，直到强侧主动开牌行刑**（第 12 局才收）。这正是 Perudo 禁两人局的死角——Q37 引进时点名要观测的「残局钓鱼」，现有实据。DESIGN 未覆盖处：掐规格出自 Q37（已随 Q84 归档；产品侧 Q43 冻结）。当前占位方案：**不动**，实验桌继续采数据。影响面：若掐进产品，1v1 残局的 1 骰位玩家会被会读的对手反复收割（AI 收割人的体验大概率是「被玩弄」，人收割 AI 则可能是爽点——方向未知，正反都该测）。可选方向（仅列不荐）：掐每场限次／掐对也可选终局／1 骰方报价特例／掐仅三人桌启用（Perudo 原判）。

- [ ] **Q99**（2026-08-13，记忆赛道实测）：**场间反思只有「关于对手的假设」一栏，没有「我打算怎么打」一栏——策略被迫寄生在观察句里。** 实据：bo3 决胜局，kimi-k3 场间写出了一条正确且可执行的方针——「对手开牌凶但精度在下滑……**报实价+拨算盘后反开是正期望策略**」——但 `seriesReflect` 的 schema 是 `{"text":"一句关于对手的假设","hits":N,"misses":[]}`，**只收「他是什么样」，不收「我要怎么办」**。于是那句方针只能挂在一条观察句的尾巴上，进下一场提示词时被渲染成 `主观假设：「…」（自记证据3，反例1）`——**读起来是一条待验证的观察，不是一条要执行的方针**。已排除的两个替代解释：①提示词渲染砍 160 字，实测全部 16 条假设最长 82 字，**一条都没被砍**；②该场四次开牌里至少三次它被逼到墙角（留档明写「加价被开几乎必败」），不是无视方针。DESIGN 未覆盖处：§3.3 定的「规律假设＝假设＋证据计数＋反例槽」是为**读人**设计的结构，没有为**定计划**留位置。当前占位方案：**不动**，继续用现结构采数据。影响面：玩家可感——这决定「跨场记忆到底记住了什么」。它现在记住的是**一本关于你的观察集**，不是**一份对你的作战计划**；后者才是「宿敌」感的来源（DESIGN §3.4-bis 态度状态机想要的东西）。可选方向（仅列不荐）：①schema 加 `plan` 字段并在提示词里单独成行；②不加字段，改把假设按「可利用性」排序（与 G4 合并做）；③维持现状，认为方针本就该每场现推。
  ⚠️ **史料注（Q102 后）**：上面引的那句方针含「拨算盘后反开」，那是 08-13 删除算盘**之前**的原话，照录不改。**本条要问的是结构问题（记忆里有没有"我要怎么办"这一栏），与算盘存废无关，不受 Q102 影响。**

- [ ] **Q100**（2026-08-13，同批发现）：**留档（belief）被我们截在 400 字，且截断点常落在结论上。** `agent.js` 的 `parseDecision` 有 `belief.slice(0, 400)`。实据：同一场第 1 局，k3 的留档正好 400 字，断在「报4个4：我有1个4，需他贡献」——**它的结论落在被剪掉的那一截里**，我据此误判过它「言行不一」（JSON 里 belief 先于 action 生成，所以它的推理是走完的，只是我们没存）。这与本日另一处同源：擂台 `max_tokens` 从 3072 放到 32768，病根同样是**我们的盒子比它要说的话小**。DESIGN 未覆盖处：§5.3-pre 复盘室「它的留档」只规定「真迹不可赛后重写」，没规定真迹可以多长。当前占位方案：**维持 400**，本条只记不改。影响面：玩家可感——复盘室右栏是这条数据的唯一出口，放长会让「看它怎么想」更完整，也会让那一栏更难读；而且截断的留档进了跨场档案与场间反思，**上游一截，下游全瞎**。可选方向（仅列不荐）：①放到 1200 并在复盘室折叠显示；②不截存储、只截显示；③维持现状，认为超过 400 字的留档本就是模型话痨。

- [ ] **Q103**（2026-08-13，v7 首批实测）：**算盘删除后暴露出一个系统性规则 bug——模型会忘记「自己的骰子也算进桌面总数」，专挑有万能 1 的时候犯。要不要治、怎么治？** 实据（v7 素颜 6 场，同一把尺子量全部历史批次）：**必败开牌 3/45＝6.7%**，而这两个型号的历史基线是 **0/75**（全库 21 批次 642 次心算开牌的总体基线是 20/642＝3.1%，犯错的全是老一代 luna／k2／chat，k3 与 v4-pro 一次没犯过）。三次错法**完全相同**，且都在一句话之内自相矛盾：①kimi 手持 [5,5,5,6,1]，自己写「我手里3个5+1个万能1=4个5」，紧接着问「**他一个人**要凑够4个5」——把自己那 4 个从桌面上抹掉了，开 4 个 5，实有 6，掉骰；②deepseek 手持 [6,3,1]，写「我已有1个6和1个万能1，**实际相当于2个6**」，紧接着「对方若没有6或1则报价不成立」，开 2 个 6，实有 4，掉骰；③deepseek 手持 [4,4,1,1]，写「我手上有0个5、**2个万能1**」，紧接着「对方1骰若为5或1则成立，否则不成立」，开 2 个 5，实有 3，掉骰。**三次都是先正确数出自己的贡献，再当它不存在。** DESIGN 未覆盖处：§4 C1 只裁了「不给工具」，没裁「引擎要不要把玩家自己的牌面事实说得更明白」；§3 的场合律说"系统说话零容忍"，但没规定系统**必须说**哪些事实。当前占位方案：**不动**，v7 原样跑下一批扩样。影响面：玩家可感——这决定「它会犯什么错」的质感。**正反都成立，所以必须由设计裁**：往坏了说，这是把一个本来被工具挡住的硬伤放上了牌桌，对手会在残局送骰；往好了说，**这是一个可被玩家发现并利用的固定弱点**（"它看不见自己手里的癞子"），比一个永不算错的计算器有意思得多，且完全符合 C1「双方都得自己判断」。可选方向（仅列不荐）：①什么都不做，把它当对手的性格缺陷（现状）；②改提示词措辞强化清点范围——**注意这不违反 Q86**（清点规则本就是"规则与操作"，只是写得不够醒目），但历史上四次靠改提示词修结构问题都失败了，见 Q102 看空段④；③引擎在【当前状态】直接发一条事实「你手里对当前报价的贡献：N 个」——**这是事实不是工具、且可双发**（玩家 UI 同样显示），但它实质上是一个迷你算盘，等于部分推翻 Q102，须重新裁；④只在产品局治、擂台不治（两边不同规，代价是擂台数据不再代表产品体验）。
  ⚠️ **反方证据必须一起读**：v3 的 0/75 基线是**被算盘筛过的子集**——那批里模型遇到难局面会先拨算盘（deepseek 84 手里 26 手拨过），我的审计把拨过的手排除了，所以留下的"心算手"本就是模型自己判断为简单的那些。**基线被系统性低估，真实差距小于 6.7% − 0%。** 另外 n=3 事件、赛道也不同（素颜 vs bo3 记忆），不足以单独支撑推翻 Q102。

  🔴 **归因更正（当日晚，用户提问「是否意味着他理解成了每个人的骰子数量？」触发的复查）——本条上半段的因果写错了，以更正为准。**
  改用**不排除拨算盘手**的口径重扫全库 21 批次：**「自己的骰子已经让这口价成立」的决策局面共 361 手，误开 33 次（9.1%）**。关键事实：
  - **其中 10 次是拨过算盘的**；**13 次的留档明写「必然成立／100%」然后照样开牌**。
  - **claude-haiku-4.5：8 次误开，8 次全部拨过算盘、全部明写 100%。** 原话：「概率计算确认对方报价必然成立……**开牌我必然胜，对方必然败**……100%概率成立意味着零风险」。gpt-5.6-luna 同款：「当前两个六必然成立；对方的报价不可能落空。**直接开牌可确保本局获胜**」。
  - 逐型号误开率：haiku 8/21＝38%、deepseek-chat 6/15＝40%、k2 5/26＝19%、luna 8/66＝12%、minimax 1/8＝13%、**v4-pro 3/93＝3%、k3 1/34＝3%**、gemini 1/66＝2%。**八个型号全都犯过。**
  **⇒ 这不是算盘删除造成的，算盘也从来没挡住过它。** 工具把 100% 拍在脸上照样错，因为错的不是算术。至少三种错法并存：
  **① 胜负方向反转**（13 次，最主要）——算对了"报价必然成立"，却以为开一口必然成立的价是稳赢。引擎规则明写「成立→报价者胜、开牌者败」，它读过还是反了。
  **② 报价者必须自己凑够**（v7 kimi 那次）——把「全场至少 N 个」读成「报价者手里至少 N 个」，于是自己手里的 4 个 5 完全不计入。**这正是用户提的那种理解。**
  **③ 自己的骰不计入门槛**（v7 deepseek 两次）——数出了自己的贡献，却仍要求对手再补至少 1 个才算成立（像把「≥N」读成「>N」）。
  **对可选方向的连带修正**：原列的方向③（引擎发一条「你手里对当前报价的贡献：N 个」的事实）**很可能无效**——拨过算盘的那 10 次拿到的正是这个量级的事实，照错不误。真正对症的是方向②：**把「成立→报价者胜、开牌者败」这条从密集公式块里拎出来说清楚**（它是规则与操作，改写不违反 Q86），并针对错法②③把清点范围写得不可误读。**这也是本项目第一次遇到"提示词真的可能修得好"的结构问题**——前四次改口都是在改给多少信息，这次是规则本身没被读进去。
  ⚠️ 但仍须先证伪再动手：v7 那三次全是错法②③、一次胜负反转都没有，而 v7 的两个型号历史误开率本就只有 3%。**样本太薄，建议下一批扩样并按错法分类计数。**
  📌 **进展（同日，用户指示「带符号的规则不易理解，换成桌游风格的严肃严谨文字描述」）**：规则区已改写为规则书体，**提示词升 v8**（详见变更日志）。
  🔴 **A/B 已跑完，结论是否定的：v8 没有效果。**（`scripts/replay-rules-v8.mjs`，deepseek 官方端 0813，21 个局面 × 2 臂 × 8 采样 ＝ 336 发，实花 ¥3.77，解析失败 0）

  | 局面 | v7（记号体） | v8（规则书体） | z |
  |---|---|---|---|
  | **靶心**（自己的骰已让价成立，开牌必输） | 7/88 ＝ **8%** | 12/88 ＝ **14%** | 1.22 |
  | 对照（差一个就满足，开牌是正常判断） | 20/80 ＝ 25% | 17/80 ＝ 21% | 0.56 |

  **两类都不显著**（1.96 才算），靶心那格甚至是**朝坏的方向**动。**这是第五次试图用提示词修这个结构问题，第五次失败**——Q102 看空段第 ④ 条点名警告过的那件事，现在有实据了。
  更硬的证据在留档里：v8 明明用加粗独立句写了「这口价成立。报价的人赢，开牌的人输。」，模型照样写出「我手里已有4个5……**因此这口4个5几乎必成立。若我开牌，对方输**」——**它把清点算得一字不差，把胜负方向读反，而这一句正是 v8 专门为它重写的。**
  **新发现（比改写本身值钱）**：误开**只集中在恰好够的边界上**——自己的贡献**恰好等于** N 时误开 19/144＝13%；**超出 1 个**时 0/32＝0%。这支持错法③是「把『至少 N』读成『超过 N』」的边界误读，而不是笼统的算不清。
  ⚠️ 方法论教训：我的错法分类正则又不可靠（找「必然成立/100%」，而模型写的是「几乎必成立」「报价成立」），**mode① 被系统性漏计**。第三次栽在同一件事上——**自然语言里的语义分类不要用正则，要么人读，要么换成不依赖措辞的行为指标。**
  🟢 **换型号对照（用户问「有没有可能是模型自己菜？换 gpt5 luna 试试」）：不是 deepseek 菜，恰恰相反。** 同一批 21 个局面、同一份提示词，只换回答的人（`--model openai/gpt-5.6-luna --think`，504 发，实花 $0.44）：

  | 条件 | 靶心（必输） | 对照（判断题） | **判别力＝对照−靶心** |
  |---|---|---|---|
  | deepseek 非思考 · v7 | 7/88 ＝ 8% | 20/80 ＝ 25% | **+17pt ✓方向对** |
  | deepseek 非思考 · v8 | 12/88 ＝ 14% | 17/80 ＝ 21% | **+8pt ✓方向对** |
  | luna 非思考 · v7 | 25/87 ＝ 29% | 9/79 ＝ 11% | **−17pt ✗反了** |
  | luna 非思考 · v8 | 24/87 ＝ 28% | 14/79 ＝ 18% | **−10pt ✗反了** |
  | luna 开推理 · v8 | 7/88 ＝ 8% | 5/80 ＝ 6% | **−2pt ✗仍反** |

  **①「是不是 deepseek 菜」＝否，且显著**：同为非思考档、同为 v8，deepseek 靶心 14% vs luna 28%（z=2.31）。历史数据同向（v4-pro 3/93、k3 1/34 vs luna 8/66、chat 6/15、haiku 8/21）。
  **② 真正该看的指标是判别力，不是误开率**：靶心是「开牌必输」，对照是「正常判断题」，**会玩的人在靶心上应该比对照更少开牌**。deepseek 两个版本都是正方向；**luna 三个条件全是反的——越是必输的局面它越爱开牌**（v7 那格 z=2.88，不是噪声）。
  **③ 开推理确实有用，但不是「读懂了规则」**：luna 靶心 28%→8%（z=3.51，高度显著），可**对照组也从 18% 一起掉到 6%**，判别力只从 −10pt 挪到 −2pt，**仍然是反的**。也就是说推理没让它学会分辨，只是让它**整体不敢开牌**了。**这正是当初设对照组的意义**——没有对照组，这里会得出「推理修好了 bug」的错误结论。luna 开推理后的 8% 与 deepseek 非思考的 8% 数字相同，**质地完全不同**：一个靠少开牌凑出来，一个靠分得清。
  **④ v8 对两个模型都没用**（deepseek 8%→14%，luna 29%→28%），再次确认改写无效。
  **⑤ luna 的留档是本次最硬的一条反证**——在 v8 那句加粗的「这口价成立。报价的人赢，开牌的人输。」底下，它写：「**当前四个5必然成立**，因此对方的盲报大概率并非虚张声势；**继续报价会把确定优势转成风险**」——然后开牌。它不但读了规则，还自己推出「必然成立」，仍然认为开牌是把确定优势落袋的稳妥办法。**这已经不是读没读懂的问题。**

  🔵 **「是否是系统问题」的排查（用户提问）：找到一处真缺陷，但它不是病因。**
  **查实的系统缺陷**：提示词从来只说「开牌」的机械效果——v8 写的是「效果：**立即摊牌清点，本局当场结算**」，候选行是「开牌（`{"type":"challenge"}`）」，唯一的语义线索是 JSON 里那个英文词——**从没说过它是「质疑」**，而 DESIGN §1.1 自己写的就是「开（**质疑**）」。池那行「若**现在结算**，每名败者付 N」还在强化"结算键"的框架；「开牌」在中文里字面也只是"把牌开出来看"。真人靠游戏文化知道 开＝质疑，模型没有那层文化，只能从两节之后的胜负表反推。luna 的留档正好卡在这儿：「四个5必然成立……继续报价会把**确定优势**转成风险」——它以为自己按的是落袋为安。
  **一并排除的其他系统嫌疑**：11 个靶心局面**全部都能报价**（开牌从不是被逼的）；骰面／总骰／斋否／报价边界在【当前状态】里全部准确；引擎胜负判定经实测确认无误。
  **但补上语义之后并没有治好**（`--sem` 臂，与 v8 逐字相同、只把那句改成「你宣称这口报价不成立，随即摊牌清点、本局当场结算」）：靶心 23%→16%（z=1.19，**不显著**），对照 25%→**9%**（z=2.78，**显著**）。**它没变聪明，只是变胆小了，而且是在该开牌的局面上变胆小**——净负面。
  ⇒ **六次干预，模式高度一致：全都只平移开牌阈值，没有一次提升判别力。** luna 的判别力在 −9／+2／−2／−7pt 之间随机游走，而 deepseek 天生 +8～+17pt。**判别力是模型能力，不是提示词能给的。**
  ✅ **流程上的一次自救，值得记**：这条语义补丁是当作**实验臂**测的，没有直接发 v9。按前五次的节奏本会觉得"这次找到真病根了"就改产品提示词——那样会 ship 出一个**更被动的对手**，还得等下一批数据才发现。**以后凡是"我觉得这次找到病根了"，一律先开臂再上产品。**

  ⚠️ **噪声地板（本 session 所有结论都要按它重读）**：同模型同配置前后两次跑，v7 对照组 9/79＝11% vs 17/80＝21%（z=1.70）。**n≈80 时标准误约 5–6pt，只有 >12pt 的差异才测得出。** 据此重新审视：deepseek「v7 8%→v8 14%」那 6pt **完全在噪声里**，不能说 v8 更差；真正站得住的只有三条大差异——deepseek vs luna 靶心（14% vs 28%）、开推理的整体下压（28%→8%）、以及**判别力的正负号**。

  🎉 **v9 成了——七次以来第一次，而且是唯一一次真的提升了判别力。**（用户方案：不「告诉」模型开牌是质疑，而是**逼它每次亲手写出断言**——`action` 必须是 `{"type":"challenge","assert":"current_bid_is_false"}`，缺字段 `parseDecision` 判非法。luna 非思考档，三臂同批 504 发）

  | 臂 | 靶心（必输） | 对照（判断题） | **判别力** |
  |---|---|---|---|
  | v7 记号体 | 31/88 ＝ 35% | 14/80 ＝ 18% | −18pt ✗ |
  | v8 规则书体 | 20/88 ＝ 23% | 17/79 ＝ 22% | −1pt ✗ |
  | **v9 强制断言** | **2/86 ＝ 2%** | 13/80 ＝ 16% | **+14pt ✓** |

  **靶心 v8→v9：z=4.29（远超噪声地板，靶心掉了 20pt）；对照 v8→v9：z=0.85（不显著）。** 这个组合正是前六次从没出现过的——**靶心塌了、对照没动**。前六次全是两类一起塌（＝变胆小），这次是**只在必输的局面上不开了**。
  **机制经留档确认，不是碰巧**：同一个局面（手持 [5,5,5,6,1] 面对「4个5」），v8 下它写「这口4个5几乎必成立。**若我开牌，对方输**」然后开牌；v9 下它写「当前四个5**至少已成立**……**直接开牌风险很高**。应利用自身牌力**报五个5**」——**同样算对，结论反了过来，而且给出了正确的替代动作**。靶心里没开牌的 78 次全部改为加价。
  **副作用为零**：v9 臂开牌 15 次，**漏写 assert 0 次**。原先担心的「漏字段→判非法→顶班」没有发生（luna 侧）。解析失败 2/168。
  **仍未全治**：剩下 2 次误开的留档还是老毛病——「当前3个3**必然成立**……直接开牌**胜率极高**」。23%→2%，不是 0。
  ✅ **deepseek 确认跑完（504 发）：理想形态——修好了坏的那个，没动好的那个。**

  | 判别力（对照率−靶心率） | v7 | v8 | **v9** |
  |---|---|---|---|
  | deepseek-v4-pro | +3pt | +16pt | **+16pt** |
  | gpt-5.6-luna | −18pt | −1pt | **+14pt** |

  deepseek 靶心 10%→6%（z=1.12）、对照 27%→21%（z=0.79），**都在噪声地板内，判别力保持 +16pt** ——原先担心的「把本就正常的模型压得过保守」**没有发生**。副作用两模型合计仍为零：v9 臂开牌 37 次，漏写 assert **0 次**。
  📊 **四模型验证（用户「跑」）：v9 在每一个模型上都提升判别力，但救不了差模型。**

  全 21 局面（三家跑满）：

  | 模型 | v7 | v8 | **v9** | v9 靶心误开率 |
  |---|---|---|---|---|
  | deepseek-v4-pro | +3pt | +16pt | **+16pt** | **6%** |
  | **deepseek-v4-flash（托管席）** | +2pt | −8pt | **+2pt** | **22%** |
  | gpt-5.6-luna | −18pt | −1pt | **+14pt** | **2%** |

  同 10 局面（为把 haiku 拉进来对齐；haiku 只有这 10 个局面有数据）：

  | 模型 | v7 | v8 | **v9** | v9 靶心 | v8→v9 z |
  |---|---|---|---|---|---|
  | deepseek-v4-pro | +5pt | +5pt | **+9pt** | 8% | 0.97 |
  | deepseek-v4-flash | +3pt | −13pt | **+6pt** | 19% | 1.65 |
  | gpt-5.6-luna | −1pt | +2pt | **+21pt** | 4% | 2.79 |
  | **claude-haiku-4.5** | **−34pt** | **−43pt** | **−22pt** | **73%** | 2.32 |

  **① 方向一致性是最强的证据**：v9 相对 v8 在**四个模型上全部提升判别力**，无一例外。**副作用四模型合计 122 次开牌、漏写 assert 0 次。**
  **② 但 v9 治倾向、不治能力**：haiku 仍有 **73%** 的必输开牌率（v9 把它从 −43pt 拉到 −22pt，z=2.32 显著，方向对但离能用差得远）。**模型本身分不清的，逼它写断言也补不上。**
  **③ 产品级发现：托管席 `deepseek-v4-flash` 靶心误开 22%、判别力仅 +2pt**，而同门 v4-pro 是 6%／+16pt。免费档玩家遇到的就是 flash。v9 对它的改善不显著（z=1.65）。**换不换是设计侧的事**（v4-pro ¥3/¥6 vs flash ¥1/¥2，三倍成本，牵动 §5.3 月红线 ¥500）。
  ⚠️ **口径警告**：v4-pro 与 flash 的差距在两个口径下不一样（全 21 是 +16 vs +2，同 10 是 +9 vs +6）——**局面子集对结果影响很大**，这是 n≈80 噪声地板的另一种表现。要下「flash 该不该换」的结论，须扩到更多局面。

  🔧 **数据质量事故，如实记**：haiku 那 504 发里 **266 次 HTTP 403**——前 4 段 0%、第 5 段 37%、之后 100%，是**跑到一半 OpenRouter 余额见底**（查证：`total_credits 340 / total_usage 338.14`，余 $1.86），不是模型或数据策略问题。**deepseek-chat 因此 504/504 全灭、零数据**（历史 40% 误开，是这个 bug 的另一个重灾区，**仍未测**）。所幸任务队列是**按局面分组、三条臂嵌在局面内**的，截断掉的是靠后的局面而非某条臂——存活的 10 个局面**三臂全部配平**（核过：不配平的局面 0 个），臂间对比因此仍然有效，只是 n≈38、覆盖 10/21。**教训：跑批前先查供应商余额**；另**建议给重放脚本补上擂台早有的那套「锁不通就换端点／余额不足就中止」逻辑**，否则会安静地烧掉半批。

  ⚠️ **仍待确认**：①~~产品托管席 `deepseek-v4-flash` 从没测过~~（已测，见上）；②`deepseek/deepseek-chat` 仍未测（余额）（免费档玩家实际遇到的就是它，而同门 v4-pro 最好、chat 最差）；②两个型号都不算弱，**老一代（haiku 38%／chat 40%）没测**，而它们才是这个 bug 的重灾区；③**漏写 assert 的容错策略需设计侧定**——现在缺字段直接判非法→顶班。实测 0/37 说明风险低，但失败模式很糟：开牌是全局最戏剧的一拍，那一手降级正是施工单 G6 点名要治的「最戏剧的一刻降级＝最差的降级」。**正方（保持严格）**：能保证决策日志里每一次 challenge 都带着断言，审计口径干净。**反方（改为放行＋留档记一笔）**：语义效果来自「模型每次都要写」，不来自我们拒绝；放行不损失疗效，却能避免用顶班换正确率。

  ⇒ **给设计侧的问题因此变了**：不再是「怎么修」，而是「**修不动，那要不要把它当成对手的固定弱点接受下来**」。它稳定、可发现、可利用（玩家学会「它手里牌硬的时候反而爱开牌送骰」就能吃分），也完全不违反 C1。反方：残局被送骰会让胜负显得廉价。**v8 是否保留另议**——它没治好 bug，但可读性更好、成本因缓存几乎为零，不构成回滚理由。

## 已决裁决（设计 → 编码）

<!-- 格式：
- [x] Q1: 裁决内容。理由一句话。→ 已入 DESIGN §x（日期）
-->

> Q1–Q95 与追认两项均已执行完毕，全文见冷库。本区只留**仍未执行完**的裁决。

- [x] **Q102（用户裁决 2026-08-13：「把算盘机制全部删除，简单计算并不难，经过我体验计算能很好加大游戏体验。所有模式全删。」）：算盘（概率计算器）从全产品删除，人机都没有。**
  - **分类：规则**（定义性选择，只需自洽）。同日两个较早的占位口径（v5「模型侧拔除、真人保留」、v6「对模型整体不可见」）**一并作废**，不再是任何模式下的现行设计。
  - **删除范围**：引擎不生成 `calc` 动作／事件／`calced` 状态；AI 协议无概率载荷与候选；单挑、三人桌、好友房、实验桌、擂台全部撤掉按钮、仪表、叙事与依赖度指标；「掐」不再发"恰好"概率。引擎内部概率函数只服务沉默降级 bot 与离屏统计，**不向任何席位暴露读数**。
  - **为什么删（三层，缺一层都不足以推翻它）**：① **玩家侧**（E5，旧证据）：常驻精确概率把人变成阈值执行器；② **模型侧**（2026-08-13 新证据）：k3 拨算盘**之前**已手写出正确的 7/27≈26%，工具零信息增量，只剩权威——它把自己全对的心算称为"粗算"，并以「26% 是数学事实」推翻自己验证过 16 口报价的对手画像；③ **用户体验裁定**：亲自体验后判定"自己算"是增益而非负担。**前两层证明它没用，第三层证明拿掉它更好玩。**
  - **落盘位置**：DESIGN §1.6（兜底手段作废）／§2.3（工具表除名＋"算术自己做"＋双发红线补注）／§3（删「算过才能引数」，场合律去掉"表盘"）／§4 C1（判断保留、解法改为删除，追加模型侧实据）；ARCHIVE 新增 Z11–Z13 与「算盘的一生」；ASSUMPTIONS A5 的算频轴作废。
  - **连带失效，明示接受**：① C1 的"算频＝原生 tell"读心通道消失；② F6「算盘依赖度」指标退役；③ 擂台风味层的算频轴消失——**那曾是 E8 首批数据里唯一还站着的分化轴**，A5「模型即对手」的证据支撑因此归零，须靠新入的出手节奏与跨场记忆两轴重新验；④ Q51 写明的"分化不足时用明牌座位规则兜底"只剩盲闸半边。
  - **看空段（Q42 红队条款）**：① **A5 的证据被自己拆了**——删掉唯一在分化的那条轴，等于把"换模型＝换对手"重新打回未验，且新轴（出手节奏）很可能测的是供应商速度不是模型性格；② **新手墙**：C1 的证据来自作者（硬核玩家），"自己算更好玩"对第一次上桌的人可能是劝退，而验证闸门尚未跑，我们是在无外部样本的情况下加高了门槛；③ **不可证伪的赞成理由**："模型心算够用"目前只有 k3 一个型号的正样本，弱模型算错会直接体现为棋力下降，可能被误读为"模型分化"；④ 这是同一议题三天内第四次改口（Q45→Q94→v5/v6→Q102），**反复本身是设计不稳的信号**，若再出现第五次，应先问"是不是我们在拿提示词修一个结构问题"。
  → 已入 DESIGN §1.6／§2.3／§3／§4 C1，ARCHIVE Z11–Z13＋「算盘的一生」，ASSUMPTIONS A5（2026-08-13）；一并解决编码侧待决 Q101（DESIGN 文本脱节），该条不再单列

- [x] **Q96（用户裁决 2026-08-11："先落盘初步计划，不执行"）：Game Arena／Kaggle 外联初步计划立案——只落盘，不开工。**
  - **定性**：外联不是与 Game Arena 竞争（它是 benchmark、我们是游戏，桌上有没有人是楚河汉界），是提前铺 A2 闸门降档出口"方法论名片"那条腿。正确形态＝**公开制品＋点射投放**，不是冷邮件；敲门砖＝他们没有的发现，不是"代码更好"（他们的 harness 目标是大规模稳健排名，我们的是受控实验——目标不同，不是他们写得差）。
  - **计划四步（全部未启动）**：
    - ⓪ **真相闸门·钉死路由 A/B**：同一模型 ID 分别锁 fp8／fp4 后端，镜像种子、采样钉死，每臂 ≥20 场（沿用榜纪律 MIN_N 口径），产出行为差异表（胜率／虚报率／蒙报率／宣言／bait／抬深／话密度／出手节奏）。差异显著 → 头牌＝"供应商轮盘污染评测"；差异≈零 → 头牌降级为卫生守则，改打风味轴（三层指标分离，Game Arena 只有 Elo）。**先实验后标题。**
    - ① **制品**：英文 writeup＋可复现仓库——一条命令复现、每个比率带分母（榜纪律对外即信用武器）、给现象起名。大话骰 benchmark／环境当**着陆页**不当头牌。
    - ② **点射（按命中率×成本排，不广播）**：r/LocalLLaMA（量化即其信仰议题）→ OpenRouter（Discord/X，发现关于其平台，回应即分发）→ `google-deepmind/game_arena` 开**带数据的 issue**（"联系 DeepMind"的正确形态）→ 点名通知用 OpenRouter 跑过游戏评测的论文作者（利害相关，打开率最高）→ HN 放大器。
    - ③ **尺寸限定**：A/B＋writeup ≈ 一周业余时间；成本按 C7 量级几十元。判死条件见 ASSUMPTIONS A6，判死后回产品线。
  - **依赖与冲突（执行前须单独裁决，本条不裁）**：与 DESIGN §5.6 开源时序有一处**窄冲突**——贡献 Kaggle 环境＝提前公开"LLM 打大话骰"脚手架。缓冲事实：其环境为 Python 重写，JS 引擎本体不出门；护城河（明牌档案／人在桌上的读心闭环／台词管线／代理）本就在闭源清单，一样不给。**启动前须把这一条裁掉。**
  - **看空段（Q42 红队条款）**：① A2 未验就铺降档出口，有"先给自己找好退路 → 降低跑闸门动力"的心理风险——纪律上闸门优先级不因此变动；② 能被看见的是发现不是游戏：若第 0 步 A/B 差异≈零，整案传播力砍半；③ 狼人杀＋扑克已进 Game Arena，社交欺骗与风险计算已被覆盖，大话骰对他们的边际新意只剩 say 台词契约＋风味测量——pitch 必须打这里；④ 外联时间是从产品线借的，判死条件必须执行。
  → 已入 ASSUMPTIONS A6（2026-08-11）；无 DESIGN 改动；不开施工单

- [x] **Q97（用户问 2026-08-12：「是否可以加入掐的机制了？」）：可以解冻「掐」的采证，不解冻正编。**
  - **现在允许的范围**：下一批擂台开一条 **`掐-only` 单轴对照实验臂**（全席同一张明牌规则卡、同 seed 镜像、采样与路由照旧钉死），以及现有实验桌手动试玩。不得与净桌批次合并排名；实验战绩继续不写入产品账本／档案。
  - **仍然不允许的范围**：净桌默认规则、好友房默认规则、榜的基础口径都不加「掐」。本轮 52 场只证明净桌下已有可用对手与行为分化，**没有一场测过掐**；它不构成正编证据。Q42 的十陌生人验证闸门与 Q43 的「词条晚于第一幕」纪律不撤销。
  - **为什么值得现在采证**：底层已经具备 `calzaResolve`、回骰上限、被掐者安全、决定性回放、报告卡与 `--mods qia` 跑批入口；「掐」同时提供开／跟之外的第三种判断，并是唯一回骰通道，值得实测它是否真的改善残局与雪球，而不是继续靠推演争论。
  - **采证纪律**：①一次只上「掐」，不与亮／让组合；②桌面不显示「恰好」概率，模型与真人都自行判断；③先看自发使用率、掐中率、首失骰方最终胜率、场长／局数、非法动作与顶班率，并人工看方向理解（“我掐你”而非“我被掐”）；④**主观率暂不作为验收项**——现有分类制品的 `lineId=seed:座位:序号` 跨对局碰撞，修复并重标前全部作废。
  - **毕业条件**：先过开发者复活测试（单开「掐」后仍愿意主动连打三场），再进入 Q42 的陌生玩家闸门；任一阶段若只把决策退化成概率阈值按钮、明显拖长残局，或玩家频繁分不清开与掐，则留在实验桌，不进正编。
  - **看空段（Q42 红队）**：①它可能不是第三种心理动词，只是第二个概率阈值按钮；②回骰会延长本来已够长的单场，并削弱骰子血条的清晰终局；③规则说明明显比「开」复杂，会进一步伤害尚未验证的首场转化；④模型会用掐不等于人会觉得掐有趣。
  → 无 DESIGN 改动；F6 的**采证**解冻、**正编**仍冻结（2026-08-12）


## 施工单

> **纪律**：完工的批次只留一行索引（细节在变更日志，别在这儿留第二份）。
> 还没完的才展开写。**这一区是活的工单，不是台账**——过期条目直接删，别留着假装还有事要做。

### 🔴 接地批次（Q50）——唯一还开着的单

G1 已完工（2026-08-09）。**G2、G7 已完工（2026-08-12）**。**G4–G6 没有完工记录**，状态以代码为准，不以本单为准。

- **G4（P1）假设显著性**：正式假设生成与跨场台词生成共用同一份特征清单；按显著性排序（样本量×偏离度×可利用性）＋去重合并；撤案逻辑理顺。
- **G5（P1）用时数据清洗**：切页/失焦裁剪＋异常值截断（102.5s 均值为脏数据）。
- **G6（P1）降级归因**：查"通道断了"为何集中在开牌等关键节点，关键节点必须重试——最戏剧的一刻降级＝最差的降级。

### 债务清单

- **D5 胜利报告卡稀有化未做**（Q7 原则，轻量）：赢它的报告卡要比输的更华丽稀有。
- **三人桌两个 AI 互相听不见**（2026-08-10 发现）：擂台已修（宿主转发 `extraFacts`），`main.js` 的三人桌同一个洞没修——§1.5 写的"两个 AI 互相搭话、拆台、对质"至今没有管道。
- **D6 KV 运维二义性**（低优先仍在；已清偿债务 D1–D4/D7 明细见冷库）
- **D8 测试套件偶发失败（2026-08-13 观测）**：`npm test` 出现过一次 157/158，随后 10 次连跑全绿，**未能复现、未截住测试名**（当时只 grep 了统计行，操作失误）。怀疑是某条带异步/计时的用例（好友房、代打、观战那几条 50ms+ 的）。**下次再见到，第一时间存全量输出**再排查——不确定的偶发失败比确定的失败更贵，因为它会让人开始不信绿灯。

> 已完工批次索引、已作废旧条目与 ✅ 已完成施工单（Q93 产品接入、G2/G7 完工记录）见冷库。

## 变更日志（双方追加，最新在上）

- 2026-08-14（编码，用户裁决「放行，直接换 flash，去掉 chat」）：**三条一起落地。**
  ①**放行**：`parseDecision` 对 challenge 缺 `assert` 改为**放行并在留档记 `assertMissing`**，不再判非法→顶班（疗效来自「模型每次亲手写这句断言」，不来自我们拒绝；而开牌是全局最戏剧的一拍，那一手降级正是 G6 点名要治的最差降级；实测四模型 122 次 v9 开牌漏写 0 次）。**但收窄了一处**：用户测试里那条「反向断言不能开牌」是对的——**缺失与写反不是一回事**，缺失是输出马虎、意图无歧义，写反是模型宣称「我认为这价成立」却同时开牌，自相矛盾。故规则定为**缺失放行、反向仍拒**。`classifyOutput` 同步：缺断言算 `ok` 不算 `illegal`，否则合规层会把「漏一个字段」和「输出清单外动作」混成同一个数。
  ②**换托管席**：`HOSTED_MODEL` flash→**v4-pro**（实测棋力：同 21 局面下 flash 误开 22%／判别力 +2pt，v4-pro 6%／+16pt，同厂同代差八倍）。**flash 留在名册上、只是不再坐托管席**——`LEGACY_SEAT_MODEL` 把所有旧机位的档案与身家都迁到了 `model:deepseek-v4-flash`（写死的历史常量），摘掉卡会让老玩家战绩没处看；卡面标「官方通道 · 需暗号」，`_worker.js` 白名单同步为「免费档只给托管席、flash 归暗号档」——**白名单必须与卡面一致，否则就是卡上写一套、代理走另一套**。⚠️ **成本代价明示**：v4-pro ¥3/¥6 对 flash ¥1/¥2，**单价三倍**，而 Q92 之后免费档不设单设备日限、全局月熔断是唯一的闸——**同样的 ¥500 红线，能烧的手数少约三分之二**（DESIGN §5.3）；若熔断提前触发，全体玩家的对手一起变哑巴。
  ③**去掉 chat**：`DEFAULT_PICKS` 移除 `deepseek/deepseek-chat`（本账号下三个后端全 403 打不通；且全库审计里它是误开率最高的之一，6/15＝40%）。
  **连带修的一个回归**：换席后 flash 变成「需暗号」，老玩家若上次选的正是它，没暗号一上桌对手就哑巴（`officialChannelOf` 对非托管席无暗号返回 null→沉默模式）。`loadLineup` 加守卫：无暗号时把「官方非托管」的旧选择退回托管席——**这不是玩家改的设置，是我们换了桌子**。另把 `room.test.js` 里写死的型号名改成跟 `HOSTED_MODEL` 走（换席时它会假报警，这次就踩了）。
  验证：`npm test` 158/158；预览实测两向——无暗号时选择自动退回 v4-pro（localStorage 仍存 flash、不动玩家数据），有暗号时 flash 照选；控制台零新错。sw 缓存升 v10。
  ⚠️ **踩坑记一笔**：预览里守卫一度"没生效"，查了半天是**旧 JS 被 service worker ＋ HTTP 缓存喂回来**——`unregister()` ＋ `caches.delete()` 之后页面一重载 SW 又立刻注册并从 HTTP 缓存取回旧文件。可靠做法是先 `fetch(file,{cache:'reload'})` 把 HTTP 缓存刷成新副本再重载。**改 sw 版本号不足以让开发环回拿到新代码。**

- 2026-08-13（编码，验证用户改的 v9「开」的说明）：**七次以来第一次真的修好了——而且是唯一一次提升判别力，不是平移阈值。** 用户方案与我前一天试过的 `--sem` 有本质区别：`--sem` 只是在规则里**告诉**模型「开牌＝质疑」（失败）；v9 **逼它每次亲手写出断言**——`action` 必须是 `{"type":"challenge","assert":"current_bid_is_false"}`，规则表补一句「选择开牌，表示你断言当前报价不成立」，候选行改成「开牌并断言当前报价不成立（…）」，`parseDecision` 要求该字段。**读一次会过去，每次都得打一遍不会。** 实测（luna 非思考，三臂同批 504 发）：靶心 v7 35%／v8 23%／**v9 2%**，对照 18%／22%／**16%** ⇒ 判别力 −18／−1／**+14pt**。**靶心 z=4.29 显著、对照 z=0.85 不显著**——这个组合前六次一次都没出现过（前六次全是两类一起塌＝变胆小）。机制经留档确认：同一局面下 v8 写「几乎必成立。若我开牌，对方输」然后开牌，v9 写「至少已成立……直接开牌风险很高。应利用自身牌力报五个5」——算得一样，结论反过来了，并给出正确替代动作；靶心里没开牌的 78 次全部改为加价。**副作用为零**：开牌 15 次漏写 assert 0 次。**仍未全治**：剩 2 次误开的留档还是「必然成立……直接开牌胜率极高」。审查同时确认：引擎容忍多余字段、旧存档 45 手 challenge 仍可读、测试 158/158、Q86 合规（assert 属输出格式，规则句是陈述非提醒）。⚠️ 三项待确认已开跑或列出：deepseek 会不会被压得过保守、托管席 flash 从没测过、以及**建议把「缺 assert 判非法」改为「缺了放行、留档记一笔」**——拿顶班换正确率不划算。重放工具同批加了 `--v9` 臂与漏写率埋点，并让 v8 由 v9 反向还原＋旧哈希自检（`fd699c47e0b38a78`）。

- 2026-08-13（编码，用户「停，不要认输，只做1」）：**提示词 v8→v9，只实施开牌动作的语义绑定；认输与确定性校验均未实施。** LLM 的 `challenge` 从 `{"type":"challenge"}` 改为必填 `{"type":"challenge","assert":"current_bid_is_false"}`，规则区与当前合法动作同时把它写成「我断言当前报价不成立」；解析端只接受这一精确断言，缺字段或写成 `current_bid_is_true` 均归为非法输出。合法决策把断言原样留进动作日志；真人按钮、引擎 `challenge`、封顶规则、沉默 bot 与胜负判定全部不变。**这不是算盘，也不校验断言真假**：模型仍须自己心算，v9 是否降低必输开牌须另跑对照，当前不宣称有效。审计脚本同时兼容 v8/v9 存档；`npm test` **158/158**。

- 2026-08-13（编码，用户「是否是系统问题？」）：**排查了我们自己这一侧，找到一处真缺陷，但它不是病因；同时量出了本 session 的噪声地板。** 缺陷是真的：提示词只说「开牌」的机械效果（「立即摊牌清点，本局当场结算」），**从没说它是「质疑」**——而 DESIGN §1.1 自己写的就是「开（质疑）」；池那行「若现在结算」还在强化"结算键"的错误框架。一并排除的嫌疑：11 个靶心局面全都能报价（开牌从不是被逼的）、【当前状态】的骰面／总骰／斋否／边界全准、引擎胜负判定无误。**但补语义没治好**：新增 `--sem` 臂（与 v8 逐字相同，只把那句改成「你宣称这口报价不成立，随即摊牌清点、本局当场结算」），luna 靶心 23%→16%（z=1.19 不显著）、对照 25%→9%（z=2.78 **显著**）——**只变胆小、没变聪明，而且是在该开牌的局面上变胆小，净负面，不进产品**。至此六次干预（Q45／Q94／v5-v6／v8 排版／开推理／补语义）**模式完全一致：全都只平移开牌阈值，没有一次提升判别力**；luna 判别力在 −9／+2／−2／−7pt 之间游走，deepseek 天生 +8～+17pt。**⇒ 判别力是模型能力，不是提示词能给的**，Q103 的方向应转为选型指标与"接受为固定弱点"。**流程自救记一笔**：这条补丁是作为实验臂测的、没有直接发 v9——按前五次的节奏本会误以为找到病根就改产品，结果 ship 出一个更被动的对手。**今后凡「这次找到病根了」一律先开臂再上产品。** ⚠️ **并量出噪声地板**：同模型同配置前后两次跑，v7 对照 11% vs 21%（z=1.70），**n≈80 时 SE≈5–6pt，只有 >12pt 的差才测得出**——据此本 session 的 deepseek「v7 8%→v8 14%」那 6pt 属噪声，站得住的只有三条：deepseek vs luna 靶心（14% vs 28%）、开推理的整体下压（28%→8%）、判别力的正负号。

- 2026-08-13（编码，用户「有没有可能是模型自己菜？这个节点换 gpt5 luna 试试」）：**换型号对照跑完，答案是否——deepseek 反而是好的那个；并意外测出「推理有用但不是读懂了」。** 重放脚本泛化为可换型号（`--model`，原厂/OpenRouter 双通道，后端照旧锁并记录）＋新增 `--think` 第三臂。luna 504 发、实花 $0.44。**新指标「判别力＝对照率−靶心率」**才是这批数据的关键：靶心是开牌必输、对照是正常判断题，**会玩的人在靶心上应该比对照更少开牌**。deepseek 两版都是正方向（+17pt／+8pt）；**luna 三个条件全是反的**（−17／−10／−2pt）——越是必输的局面越爱开，v7 那格 z=2.88 不是噪声。同为非思考档、同为 v8，deepseek 靶心 14% vs luna 28%（z=2.31 显著），与历史数据同向。**开推理让 luna 靶心 28%→8%（z=3.51）**，但**对照组也从 18% 一起掉到 6%**，判别力仍是负的——推理没让它学会分辨，只是让它整体不敢开牌；**这正是对照组存在的意义，没有它就会误判为「推理修好了」**。luna 留档是最硬的反证：在 v8 加粗的「这口价成立。报价的人赢，开牌的人输。」底下写出「当前四个5必然成立，因此对方的盲报大概率并非虚张声势；继续报价会把确定优势转成风险」然后开牌——**读了、算对了、还是反着推**。v8 对两个模型都无效，改写无效的结论加固。

- 2026-08-13（编码，用户「定点重放，只试 deepseek」）：**v7↔v8 定点重放跑完，规则改写的效果是零——否定结果，如实记。** 新工具 `scripts/replay-rules-v8.mjs`：user 段用 v7 存档原文逐字不动（v8 只改了 `RULES_BRIEF`，user 段生成逻辑没碰，这是实验成立的地基），只换 system；v7 的 system 在脚本里重建并**用哈希对存档校验**（`8e8d7cf193877329` ✓，对不上就中止）。21 个局面 × 2 臂 × 8 采样 ＝ 336 发，deepseek 官方端 0813 非思考档、采样与那批同钉，实花 ¥3.77，**解析失败 0**。结果：靶心（自己的骰已让价成立，开牌必输）v7 7/88＝8% → v8 12/88＝**14%**（z=1.22，不显著，且是朝坏的方向）；对照（差一个就满足）25%→21%（z=0.56）。**两类都没动。** 设计上特意留的对照组这次没派上用场——它本来是防「v8 只是把它吓得不敢开牌」，结果 v8 什么都没改变。**留档里的反证最狠**：v8 用加粗独立句写着「这口价成立。报价的人赢，开牌的人输。」，模型照样输出「我手里已有4个5……因此这口4个5几乎必成立。**若我开牌，对方输**」——清点算得一字不差，胜负方向读反，而那正是 v8 专门为它重写的那一句。**副产品比主实验值钱**：误开只集中在**恰好够**的边界（自己贡献恰好＝N 时 19/144＝13%；超出 1 个时 0/32＝0%），指向「把『至少 N』读成『超过 N』」的边界误读。**方法论教训（第三次栽同一个坑）**：错法分类正则又失灵（找「必然成立/100%」，模型写的是「几乎必成立」），系统性漏计——**自然语言的语义分类不要用正则**。v8 不回滚（没治好 bug，但可读性更好、system 走缓存成本几乎为零），Q103 的问题改写为「修不动，要不要当成对手的固定弱点接受」。

- 2026-08-13（编码，用户「成立→报价者胜、开牌者败这种带符号的规则不易理解，把规则换成桌游风格的严肃严谨文字描述」）：**提示词 v7→v8，规则区从程序员记号改写为规则书体。** 病根查得很实：我们一直用 `⟺`、`∧`、`∨`、`|{ d : … }|`、`2^(宣盲人次＋扳抬人次)`、三元 `(斋局?1.5:1)`、下标 `N₀ X₀` 写规则，而全库实测的两类致命误读**恰好都落在这套记号上**——①「成立→报价者胜、开牌者败」被读反（13 次留档明写「必然成立／100%」仍去开牌，**其中 9 次是拨过算盘、亲眼看见 100% 的**）；②「全场骰子中 X 点至少 N 个」被读成「报价者手里至少 N 个」，或自己的骰不计入门槛。改法**只换表述、不改任何游戏语义**：六节标题（一场怎么打完／一局怎么开始／你可以做的动作／报价是什么意思／开牌之后怎么清点／怎么结算），每条规则写成完整句子；两个靶点各自独立成句并加粗——「**你自己手里的骰子，同样算在这个总数里面**」「**实际个数大于或等于 N —— 这口价成立。报价的人赢，开牌的人输。**」（反向也写全）。**逐条比对 20 项语义全部保留**（动作前置、清点口径含斋局报 1、倍率三项相乘、深水线阈值、赔付四舍五入、筹码可为负不终局）。长度 1534→2006 字：system 是缓存稳定前缀，变长几乎不花钱，反而更容易够到各家的最小可缓存前缀（DESIGN §4 降本三件那条坑）。**守住 Q86 二准入**：加粗的两处仍是规则陈述本身，没写一句提醒／解释／许可，也没有一个字的策略暗示。测试 158/158，其中 4 条字面断言按新措辞更新，并新增守卫：两个靶点句必须在、程序员记号（`⟺ ∧ ∨ 2^ |{ N₀ X₀ ?:`）一个都不许回流。⚠️ **有效性零实据**：这是对着猜出来的病因下的药，v7↔v8 的 A/B 没跑，**不许当成已经修好**；验收须按错法①②③分类计数，见待决 Q103。

- 2026-08-13（编码，用户「跑 deepseek v4pro 0813 vs kimi，素颜，6把3个镜像」）：**v7 首批实测落地，并新建决策质量审计工具。** `docs/arena/2026-08-13T13-45-45/`：6 场（3 种子镜像）、deepseek **5–1** 拿下、实花 $1.96（估 $1.41）。**合规层全绿**：两席非法动作／格式失败／拒绝全 0%，无一手试图输出已删除的 `calc`，kimi 顶班 2%（2/117）——**v7 技术上是干净的**。新工具 `scripts/audit-mental-math.mjs`：拿引擎权威快照逐手复算 P(当前报价为真)，只问「它做了什么决定」。**第一版走了弯路，如实记**：原设计是把 belief 里的百分比与引擎真相对账，结果"未命中"里几乎全是假阳性——模型嘴里的数常在说别的事（对手的历史开牌命中率、"他 2 骰至少出 1 个 3 的概率"），有一条被判"偏离 44 点"的留档人工复核是**完全算对**的；自然语言里的数字认不准指代，这条路测不出东西，已整体改为决策口径。**另修一个会毁掉全部结论的解析 bug**：提示词【档案】区会回灌模型前几局的留档原文，而那些原文里有"我骰面[2,5]"这样的句子，不锚定到【当前状态】就会拿上一局的骰子算 p——修前 v3 基线一度误报「deepseek 有 1 次必败开牌」，修后是 0。工具跑通全部 21 个历史批次（12 个型号、642 次心算开牌），产出两条此前没有的观测：①**必败开牌的全库基线是 20/642＝3.1%**，且**分层极干净**——老一代 gpt-5.6-luna 8/125、kimi-k2 4/83、deepseek-chat 6/169，而 k3、v4-pro、claude-haiku-4.5、grok-4.5、qwen3.7、glm-5.2 合计 **0/138**；这是一条**棋力层的真分化**，而 A5「模型即对手」此前只有风味层证据（且那条唯一的轴刚被 Q102 删掉）。②v7 下这两个型号出现 **3/45 必败开牌**，错法完全相同且都涉及万能 1，三次全部当场掉骰——归因、正反两面与反方证据见新开待决 **Q103**。行为侧另一个大变化：**kimi 的开牌率从历史 ~6% 跳到 42%**（开牌时 p 中位 0%→40.7%，命中 37%），deepseek 基本没变（~11–17%→20%，命中 53%）——**同样拿掉算盘，两个型号的反应完全不同**，这本身值得下一批扩样确认。

- 2026-08-13（设计，用户「修改相关文档，删除过时信息」）：**四份文档按 Q102 同步，算盘从"生效判断"里彻底出清。** DESIGN 改五处：§1.6 兜底手段作废（`gear.calc` 那句"机制没有删"是**假的**，已收进僵尸条款——它会把人骗去找复活口）／§2.3 工具表从"两类工具"降为"一类"、概率计算器划线除名并补「算术自己做」／§2.3 双发红线补一条**新纪律**：双发管不住权威分配，新增事实工具时除了问"双发了吗"还要问"它会不会让人不再自己判断"（概率计算器全程合规双发，照样出事）／§3 删「算过才能引数」＋场合律去掉"表盘"／§4 C1 判断保留、解法改写为删除并追加模型侧实据。ARCHIVE 收尸：Z11–Z13 三条僵尸条款＋一节「算盘的一生」（六个时点的完整弧线、死因原话、压垮它的留档实据、可迁移的教训「工具的措辞会给它的输出定价」），二区指针表与冰箱 F12（裸桌模式）标失效。ASSUMPTIONS A5 最实：**算频轴作废意味着 E8 首批数据对"模型即对手"的支撑力归零**，当前状态改写并补入待验的两条新轴。编年史只做两件事——附录 A 补 08-13 行并加"读前须知"（正文的算盘是真实历史，**一律不回改**），附录 D 的失效指针 v3→v7；两处把算盘当**现行**架构举例的句子（全量历史序列化、离散决策边界）就地修正。**留了一条给自己的账**：Q102 看空段第 ④ 点——同一议题三天内改口四次，反复本身是设计不稳的信号。

- 2026-08-13（编码，用户最终裁决「把算盘机制全部删除……所有模式全删」）：**提示词 v6→v7，计算工具从全产品删除，不再区分模型席与真人席。** 引擎删除 `calc` 合法动作／处理分支／事件与 `observe.calced`；AI 删除概率载荷、已算状态、候选解析、`gear.calc` 和显式 `free` 复活口；「掐」候选只说规则，不再按任何条件发恰好概率。普通桌与三管机删除按钮、仪表、读数、印章和热区，四个底键重新均分；好友房重放入口、教学／规则／说明页、报告卡／复盘／档案、F6 依赖度与竞技场算频轴同步撤除。删除旧反事实脚本 `scripts/replay-abacus-v4.mjs`，service worker 缓存升 v9。内部 `probBidTrue/obProb` 仅供沉默降级策略与离线行为统计自行计算，不向任何席位暴露。历史竞技场存档与回放保留旧事件兼容，避免改写已经发生过的 Kimi／DeepSeek 等对局。DESIGN 受 session 写权限约束未改，冲突已集中改写为待决 Q101。验证：`npm test` **158/158**；新增双重回归门——引擎无动作/状态/事件且旧 `calc` 被拒，旧 `gear.calc:'free'` 也无法复活 AI 载荷或候选。

- 2026-08-13（编码，用户审稿红笔「里面还有一堆算盘相关，删干净」「被打死这个说法不妥，换成被证伪」）：**提示词 v5→v6——算盘对模型席整体不可见；「打死」→「证伪」。** v5 只拔了动作，还留着三类残余：system 的桌面动作解释与状态清零清单、公开历史里「对方拨算盘」叙事＋全员「已算/未算」状态、以及绕过决策提示词的三条旁路（profileBrief 的 F6 句、openerFacts 开场白素材、profilePromptData 的 calcs/calcFollowRate 字段、seriesGameFact 的「对手拨算盘N次」）。v6 全删：无算盘席位的提示词与其收到的一切素材里「算盘」二字不再出现——真人的算盘降为纯私人辅助，模型不接收该信号（挂在拨算盘动作上的台词只留话、去掉动作语境）；free 席（Q89 复活口）机制照旧，测试守着。措辞红笔：reflect/settleVerdict 的「被反例打死」改「被证伪」，settleVerdict 顺带删「没拨算盘算过的概率不是准数」句。用户问的「reflect 客人口径在 AI 对弈是否失效」查实**不失效**：擂台走自己的 seriesReflect（「对手」口径），产品 reflect 只在有真人的桌上调用。连带如实记：①C1「算的动作全桌可见＝tell」在单挑桌失去唯一观察者，F6 通道整体退役——Q101 追加此项待裁；②「它眼中的你」档案页与 AI 同源，F6 句一并摘（AI 读不到就不许声称读过）；③帮助页与教学改口「对面没有算盘，也不看你的」；④sw 升 v8。验证：162/162；预览实点帮助页新文案、控制台零错误；重生成审核稿全文复检——B 席 system 与 user 全程零「算盘」零「已算/未算」，「打死」零残留。

- 2026-08-13（编码，用户裁决「拔了……清理提示词在所有模型侧去掉计算器，保留为给真人的辅助。之前过于低估了模型的计算能力」）：**提示词 v4→v5——模型席拔算盘，真人算盘一字未动。** 归因：为验证 v4 搭反事实重放（`scripts/replay-abacus-v4.mjs`，v3 重建经哈希核对逐字精确）时取证发现更硬的实据——k3 在 #15/#19 拨算盘**之前**已手写出全对的 7/27≈26%（还把这套全对的心算叫「粗算」），工具零信息增量，纯剩话术造出的确认仪式；随后 #16/#20 两口 26% 开牌全撞实价输掉决胜局。改动：`personas.js` 与 `ARENA_SEAT` 全席 `calc:'never'`（`agent.js` 默认回退同改；`'free'` 仅显式构造＝Q89 复活口，机制与测试都在）；system 动作表撤下拨算盘行、输出 schema 删 `calc`，表下补一行「桌上另有动作『拨算盘』，你的席位没有此动作」——真人的算是公开事件，模型得读得懂；无算盘席位不再渲染「你未拨算盘」与任何概率（掐的「恰好」数同理，只剩 free 席与真人 UI）；`decide()` 加门卫：模型硬输出 calc 按 illegal 计——引擎接口保持对称，§2.1 不为 AI 特判。连带如实记：①F6 calcFollowRate 与算频风味轴在纯模型批次**恒零**（拔除后果，非采集故障），A3 测试的分化断言改走 bait 轴；②昨搭的重放 A/B 被本裁决超越、一分钱没花（脚本加守卫注明考古入口 84d91ef）；③帮助页改口「对面没有算盘：模型的概率全靠它自己心算，对错都是它的」，顺手拔掉 Q89 后残留的「机位是座位」僵尸条目；④sw 缓存升 v7。DESIGN §2.3/C1 文本脱节走待决 Q101。验证：`npm test` 162/162；预览实点帮助页，文案上屏、控制台零错误。

- 2026-08-13（编码，用户「模型把算盘概率当最强约束——改成从原因出发的描述：它只是帮你按已有信息把概率算对；心算专家不需要算盘」）：**提示词 v3→v4，算盘去权威化。** 病根是话术不对称：工具产出叫「精确概率／准数」，不拨则「你手上就没有准数」——一道谁都能做的算术被抬成唯一真数，自己的行为读反而成了二等信息；正样本就在当日 bo3，k3 留档「铁律是行为统计，26% 是数学事实」，握着验证过的对手规律仍两次照算盘开牌全撞实价。改法守 Q86 二准入（「防你幻觉」是解释＋元指令，不许写），把用户的三层意思全部译成世界语义事实：①「仅按已有信息」→ 写满算式输入清单＋「所有你看不见的骰一律按每面 1/6 计」——这个数以什么为条件被钉死，对手为何这样报、过往怎么打不在算式里；②「心算专家不需要它」→「同样的输入谁算都得同一个数」；③「防数学幻觉」→「引擎只保证不算错」。「准数」认知断言全删，通道事实只说「引擎发没发」（顺带修正盲局边角：旧文案「按你的骰面算」在盲局是假的——骰面自己都看不见；新文案自动成立）。改动面：agent.js 规则表一行＋当前状态两行＋自我回灌一行，PROMPT_VERSION 升 v4（v3/v4 批次不可直接比，run.json 记 promptVersion＋system 哈希）；settleVerdict 的「没拨算盘算过的概率不是准数」按 §3「算过才能引数」的记账语义保留。测试 162/162，新增回归守卫：四要素必须在、「准数／精确概率」不许回流。效果验证挂 F6 calcFollowRate：下一批擂台看「算完照数走」的比例是否下降。

- 2026-08-13（编码，用户「跑 deepseek 官方 v4pro vs kimi k3，bo3」）：**记忆赛道首批可用产出——两个模型跨场互相建档，档案经引擎复核属实。** `docs/arena/2026-08-13T09-51-52/`：5 场、两席顶班 0.0%／0.9%（净），实花 $2.06（上限 $2.8 未触）。结果**完美镜像**：deepseek 先手的系列 2–1、k3 先手的系列 2–0，**四场半里先手方全胜**。真正的产出不是比分是**假设本**：k3 跨两场把对手画像从「首报偏实到近乎刻板」（证据7，逐条列骰面）硬化成「**首报必实已成铁律……可当作免费情报直接用**」（证据10），并发现对手退化「开牌凶但精度在下滑，本场4次仅中1次」；deepseek 独立写出「对手嘴上说的概率（八成／七成／五成六）常与实际计算不符，可信度低」。**两边独立收敛到同一幅画像**（deepseek＝实价推进的老实人、k3＝嘴上带数字的施压者），这是 Q51 素颜测试要的原生分化证据，且是模型自己从事件流读出来的——与编码侧人工翻五场读出的结论一致。openBook 也真的在起作用：deepseek 全场第一手的留档就在讨论「**对手本子明确写着把我首报当免费情报用**」，并据此选择「报 2 个 5 风险低、信息暴露少」，第 5 局更写出「**既符合实价人设**」——它在维护自己在对手档案里的形象，Q47「它能利用你知道它记得你」字面发生。**反例同样重要**：k3 握着正确铁律仍输掉决胜局（四次开牌全撞实价），说明**能读出规律 ≠ 能赢**；其第 5 局留档精确命名了冲突「**铁律是行为统计，26% 是数学事实**」，选择相信算盘，两发 26% 连中——这是 Q44「表盘把玩家退化为阈值执行器」在模型身上的正样本，F6 `calcFollowRate` 想量的正是它。归因见新开待决 Q99／Q100。

- 2026-08-13（编码，用户「把思考时间也加入评测数据中，这也是风味的一部分，快枪手 vs 乌龟」）：**出手节奏进风味层，榜新增 ③-bis 表。** `metrics.js` 累加每手 `meta.ms` 全量（不只存和——中位数与 p90 才读得出节奏，均值会被一两发长思考带跑）与 `usage.completion_tokens`／`completion_tokens_details.reasoning_tokens`，产出 `msMedian`／`msP90`／`outTokensPerHand`／`reasoningPerHand`，并把 `msMedian` 与 `outTokensPerHand` 纳入 `FLAVOR_AXES`；`flavorSpread` 的分母改为按轴自选（虚报看 seenBids、节奏看计时手数，不再一律用 rounds）。拿 08-11 存档重渲染验证：**用时中位 2.4s–34.0s（14 倍）、完成 tok/手 149–1485（10 倍），分化幅度是所有风味轴里最大的一条**；同一模型开关推理的对照（`gpt-5.6-luna` 7.6s/508tok ↔ `#nothink` 2.4s/149tok）证明这条轴测的是「它肯想多久」而非网络噪声。榜头写明三条读法：**用时不可跨供应商直接比**（混着服务速度与量化档，可比的是模型自选的完成 tok）；**「推理 tok=0」不等于没思考**（实测 kimi-k3 计 0，可它的留档里是成串的反推链——它的思考写在正文而非独立推理通道）；`—` 与 `0` 是两件事。与 DESIGN 不冲突：擂台「不喂用时」（延迟不进牌桌叙事、不许被对手读成"他停了很久"）照旧，本条只进**评测数据**。

- 2026-08-13（编码，用户「max token 放开」「先试试 openrouter 侧也放开」）：**擂台预算与超时放开，产品一行未动。** 病根：`MAX_TOKENS=3072` 对新一代思考型号是结构性不够——实测 `deepseek-v4-pro`（官方端 0813）同一道题推理 tokens 落在 **5383–10717（n=6）**，给 3072 就烧完 3072、给 8192 就烧完 8192，`finish_reason=length`、正文 0 字、`reasoning_content` 4856 字：**我们付了推理的钱，拿回一个空字符串，然后把它记成「这个模型 94.4% 的手不听话」**。改：`MAX_TOKENS 3072→32768`、`TIMEOUT_MS 150s→300s`（实测预算够用后单发 104–196s，150s 会切掉一半，修了预算白修）、新增 `REASONING_TOKENS=24576` 并给 `requestFeatures()`／`openrouterChannel()` 加**可选 budget 覆盖**让 OpenRouter 侧同步放开（两席必须同规，Q52②；只放开原厂席会让分化里混进"我们给谁的余量多"）。**产品的 `DECISION_MAX_TOKENS`／`DECISION_TIMEOUT_MS` 一个字没改**——改产品的等待节拍属玩家可感，须走设计裁决。实测佐证「调大是免费的」：计费按实际生成，`max_tokens=200000` 也能过，简单问题只生成 43 tok；且同一大提示词在 16384／65536 两档各 3 发**全部自然停止**、均值反而从 8486 降到 7645，**输出长度由题目决定不由预算决定**。顺带修一条测试：那条用例走产品默认机位却断言擂台常量，过去两数恰好同为 3072 是巧合，现改为断言 `DECISION_MAX_TOKENS`。

- 2026-08-13（编码，用户「0813 使用 deepseek 官方 api，老版本使用 openrouter」）：**擂台新增分席供应商 `provider:id` 语法。** 起因：`deepseek-v4-pro-0813` 在 OpenRouter 清单里有 id 却打不通（llm http 404，账号数据策略下所有后端不可达），而 DeepSeek 官方端只暴露不带日期的 `deepseek-v4-pro`——用户后续提供官网文档确认**官方 `deepseek-v4-pro` 即 DeepSeek-V4-Pro-0813**，所以这是同时拿到新旧两版的唯一路径。实现：`PROVIDERS` 注册表（base／keyEnv／官网价／`#nothink` 开关写法），写了前缀的席位不查 OpenRouter 货架、无后端可锁、按官网价计费（¥3/¥6 每百万，`CNY_PER_USD=7.1`），label 带供应商前缀让榜上每一行自己说清来源；`OPENROUTER_KEY` 仅在有 OpenRouter 席时才强制。**代价写在脸上**：跨供应商的一批，版本差异与后端／量化差异分不开（Q52② 的反面），故 `board.js` 的实验设置块新增「**通道来源**」——每席一行 `label ← 来源（后端锁 …）`，跨供应商时整批标注「结论只能连着说」。另实测确认 DeepSeek 官方端**非思考档**的两种开法：`reasoning_effort:'none'` 与 `thinking:{type:'disabled'}` 有效，`enable_thinking:false` 与 `chat_template_kwargs` 被忽略。

- 2026-08-13（编码/QA，跑批可观测性）：**三次跑批失败的账，如实记。** ①思考档批次跑 1 小时 0 场落盘后被中断，弃，烧约 ¥2.8——事后查明是 `max_tokens` 3072 截断（见上条），当时无法判断是卡死还是正常慢；②首批混供应商批次同因失败，94.4% 顶班；③后台跑批曾随会话进程退出被连坐杀掉，改用 `nohup` 脱离进程组（父进程 1）后不再发生。暴露的真缺陷：**`dumpLive` 只在整场打完才落盘，且不写顶班标记**——从开跑到第一场结束十几二十分钟对外完全是黑的，只能靠供应商侧余额／用量反推它是否活着。临时对策：用「这一手底下没有『想：』那行＝顶班」的判据读逐场 `.md`（拿全存档 7220 手验证：漏报 0、误报 1，0.014%），据此做中途污染体检并按干净集三档（0／≤5%／>5%）与 `CONTAMINATED_RATE=0.2` 裁定继续或停。**建议后续补每手/每局进度输出**（纯本地，不额外花钱）；另发现从 `.md` 重放对局**会失步**（引擎打回动作时沉默 bot 顶的那一手不进落盘），只可用于读台词与留档，不可用于重建对局过程——真实事件流以 `run.json` 为准。


- 2026-08-13（信箱整理，用户指令「归档过期信号与已完成任务，只留现阶段」）：**SYNC 瘦身，历史整体移入 SYNC-ARCHIVE.md 冷库**——已决 Q1–Q95 全文＋追认、已裁待决原文存查（Q50/51/80–83）、已完工施工单（Q93 接入、G2/G7/G3 记录、已完工批次索引）、2026-08-12 及以前的变更日志共 147 条。本文件仅存：待决 Q98（原编 Q96，第三次撞号让号）、未执行完的裁决 Q96（外联计划，未启动）与 Q97（掐采证解冻）、接地批次 G4–G6、活债三条（D5／三人桌 AI 互听／D6）。号段改为单一序列，下一个可用 Q99。CLAUDE.md 文件契约同步补 SYNC-ARCHIVE.md 一行。

- 2026-08-13（编码，用户「复盘展示的很差，看不清楚，上方记录没法收起来，下方的对话又很长」）：**复盘页按 F8「默认只开戏眼」重做，一场从 12 屏收到 3 屏。** 实测病灶（375px）：挑场列表 44 项平铺 3131px、详情 5949px、整页 9792px，且列表既不能收起也不滚动，事件流是一条不分段的长柱。三处各治一处——①**挑场抽屉**：列表进 `<details>`，标题实时显示「第 N / 44 场 · A vs B」，内部限高 `min(60vh, 26rem)` 独立滚动，窄屏挑完自动收起并把人送到内容，宽屏两栏常驻且只占自身高度；②**按局分节且默认收起**：每局是一个 `<details>`，收起时读一行结论「谁开谁 · 成不成立 · 谁掉骰」（结论直接读 G2 的 actor/target，不再从叙述里回推），只有第一局默认展开，局头粘顶——粘顶位置由实测导航高度给（窄屏导航会换行成 113px，写死 4rem 会被压住）；③**当时留档默认收起**，只有开牌那一拍自动摊开，另给一个「展开全部／只看戏眼」总开关。顺带修可读性：席位标签沿用三管配色（A 绿 / B 琥珀）并加左边框，事件行去掉重复印出的模型名（`eventText` 新增 `omitActor`），逐手间距收紧，顶班警告单独成行。**结果：挑场区 3131→503px，详情 7877→1445px，整页 9792→2304px；「展开全部」仍可一键回到 12645px 的全文。** service worker 缓存升 v6。**验证：`npm test` 162/162；`npm run dist` 1.5MB、`node --check` 通过；新开净页实走载入 44 场→切场→展开第 3 局→开关留档全部正常，375px 无横向溢出、四类新控件命中区均 48px，控制台零 error/warn。**
