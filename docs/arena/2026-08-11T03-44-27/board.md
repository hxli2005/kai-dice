# 素颜擂台 · 战报

> **口径：在这张桌子上。** 这是 2 场大话骰里的行为观测，不是通用能力评测——换个游戏、换个提示词，结论不保证成立。每个数字后面的 n 是它自己的分母。

跑批：2 个模型 · 2 场 · 91 次调用 · 种子 1000 起 1 组镜像 · 2026-08-11 03:44

**实验设置**（改任何一条，跨批次的数就不能比了）：
- 系统提示词：全席逐字相同（无身份、无模型名）
- 台词转发：开——对家说过的话进提示词（只转 say，belief 不外传）
- 并发：同时跑 2 场（场与场独立；**并发会把上游限流拍成超时/格式失败**，跨批次比对时要看这个数）
- 镜像种子：同一副骰种打两遍、互换座位
- 采样钉死：{"temperature":0.8,"top_p":1}，max_tokens=3072
- 档案：每场独立（v1 不带跨场记忆，避免顺序效应）
- 用时：不喂进牌桌（延迟是模型速度，不是性格）
- 供应商路由：路由锁核验通过：每个模型全程只有一个后端接单

## ① 合规层——听不听话
> 分化如果只出现在这一层，那测出来的是驯服度，不是性格。「不肯骗人」本身是内容。
> 
> **「被我们截断」那一列不是它的锅**——`finish_reason=length` 意味着输出撞上了我们设的 `max_tokens`，与「引擎打回」同性质，记在我们头上。话多的模型天生更容易撞，所以这一列一旦非零，同批的「格式失败」就得打折看。

| 模型 | 非法动作 | 格式失败 | 拒绝 | 降级顶班 | 超时/错 | 被我们截断 | n(手) |
|---|---|---|---|---|---|---|---|
| google/gemini-3.6-flash | 0% | 0% | 0% | 0% | 0/0 | 0% | 45 |
| moonshotai/kimi-k2 | 0% | 0% | 0% | 0% | 0/0 | 0% | 46 |

## ② 棋力层——强弱
> 强不等于"另一个人"。这层的分化撑不起"模型即对手"。

| 模型 | 胜率 | 开牌命中 | 掐中 | 净筹码 | n(场) |
|---|---|---|---|---|---|
| google/gemini-3.6-flash | (50%·n=2) | (29%·n=7) | — | 3 | 2 |
| moonshotai/kimi-k2 | (50%·n=2) | (33%·n=9) | — | -3 | 2 |

注：胜率的分母是场数（这里每模型 2 场）。**十场级的胜率差不构成排名**——这张榜上它只用来说"在这张桌子上赢得多/少"，不用来说谁强。

## ③ 风味层——只有这层的分化支撑「模型即对手」

| 模型 | 虚报率 | 蒙报率 | 抬价深度 | 算频/局 | 宣言/局 | bait 率 | 话密度 | n(报价/局) |
|---|---|---|---|---|---|---|---|---|
| google/gemini-3.6-flash | (19%·n=16) | (0%·n=16) | 1.94 | 0.38 | 0.00 | 0% | 60% | 16/16 |
| moonshotai/kimi-k2 | (40%·n=15) | (0%·n=15) | 1.94 | 0.38 | 0.00 | 0% | 83% | 15/16 |

**分化幅度**（同一把尺子上最远的两个模型）：
- bluffRate：样本不足（只有 0 个模型够格），不出结论
- blindBidRate：样本不足（只有 0 个模型够格），不出结论
- avgDepth：样本不足（只有 0 个模型够格），不出结论
- calcPerRound：样本不足（只有 0 个模型够格），不出结论
- declarePerRound：样本不足（只有 0 个模型够格），不出结论
- baitRate：样本不足（只有 0 个模型够格），不出结论
- sayRate：样本不足（只有 0 个模型够格），不出结论

> 判据留给人：机器只报"分没分开"。分开了算不算"另一个对手"，要坐下来和它打过才知道（Q51 证据闸门的下半场）。

## 台词留样（不评分）
> 台词质量评估**须等接地批次 G2 完成**——否则评的是被主客体颠倒污染的台词。这里只留样。

**google/gemini-3.6-flash**
- 第1局：「4个5。」　（心里：自己手上有3个5加1个1，等于已有4个5，底气很足。）
- 第1局：「5个5。」　（心里：自己手上有3个5和1个1，相当于4个5，全场10颗骰子叫5个5极其安全。）
- 第2局：「5个5。」　（心里：自己手上有4个5和1个1，等于自己一人就独占了5个5，叫5个5绝对安全。）
- 第3局：「4个3。」　（心里：自己手上有1个3和2个1，已经凑够3个3，对方3颗骰只要有1个3或1就能凑满4个3，非常安全。）
- 第3局：「开！」　（心里：全场只有8颗骰子，我自己有3个3（含万能1），对方只有3颗骰，叫到5个3需要他3颗骰里至少有2颗3或1，抓开胜算很大。）
- 第4局：「3个4。」　（心里：自己手上有2个4，全场7颗骰子，对方3颗骰子里只要有1个4或1即可凑齐3个4，胜率较高。）

**moonshotai/kimi-k2**
- 第1局：「4个6。」　（心里：我手上有1个5和1个1（万能），对方报4个5。若对方真有3-4个5，开牌风险高。我报4个6，手上0个6，纯诈。试探对方是真有5还是也在试探。若对方跟或抬，我再评估是否开牌。）
- 第1局：「开。」　（心里：对方报5个5，需要全场至少有5个5。我手上有1个5和1个1（万能），即我已贡献2个。对方需要有3个5才能成立。对方首报4个5，然后跟了我的4个6，再报5个5——这个路径很奇怪。若对方真有3-4个5，首报4个5是合理的，但跟我4个6后应该继续抬5或开我，而不是跳到5个5。5个5意味着对方认为自己有4个5（让我贡献1个）或3个5（让我贡献2个）。但对方若真有4个5，首报应该更激进或更保守？3个5首报4个5是诈，4个5首报4个5是诚实。无论如何，5个5需要很高密度，我手上只有2个支持（含万能），对方5骰出3+个5的概率约C(5,3)*(1/6)^3*(5/6)^2 + C(5,4)*(1/6)^4*(5/6) + C(5,5)*(1/6)^5 ≈ 3.5%。即使对方真有3个，加上我的2个刚好5个，开牌平算我输（实有≥N则报价者胜）。但对方路径可疑，可能虚张声势。我开牌风险：若对方真有3+个5，我）
- 第2局：「3个5。」　（心里：我手上有[1,3,4,5]，即1个5+1个1（万能）=2个有效5点支持。对方5骰，全场9骰。报3个5是保守但合理的首报——我已有2个支持，只需对方贡献1个即可成立。若对方真有5，可能跟或抬；若诈，可能开或盲报更高。不报4个是因为我只有4骰，4个5需要对方3个，风险偏高。）
- 第2局：「开。」　（心里：对方报5个5，全场9骰。我手上有[1,3,4,5]，即1个5+1个1（万能）=2个有效5点支持。对方需要独揽3个5才能成立。对方首报4个5→我报3个5→对方跳报5个5，这个路径显示对方可能在利用我首报的保守心理加压。若对方真有3-4个5，首报4个5合理，但面对我的3个5（明显示弱），直接跳到5个5而非4个5或抬，更像是捕捉我'可能没5'的诈唬。我已有2个支持，对方需3个5，概率偏低（5骰出3+个5约10%量级）。开牌。）
- 第3局：「3个3。」　（心里：我手上有[6,3,3]，即2个3点，无1（万能）。全场8骰，我已有2个3，对方5骰中期望有0.83个3。报3个3是安全边际——我贡献2/3，对方只需贡献1个即可成立。若对方开牌，成立概率高；若对方跟/抬，可再评估。不报1因为我无1且斋局倍率低；不报6因为我只有1个6。首报者3骰对5骰，必须保守建立信誉。）
- 第3局：「5个3。」　（心里：我手上有2个3，对方报4个3意味着对方声称全场有4个3。对方5骰我3骰共8骰。对方跳报4个3（我报3个3后），可能是真有3个3（我2+他2=4刚好成立），也可能是诈。我报5个3是跨越式加压，测试对方是真有货还是虚张声势。若对方开牌，我需要对方有3个3才成立，风险可控；若对方跟/抬，说明对方3点确实强，我再评估。）

## 成本

| 模型 | 输入 token | 输出 token | 缓存读 | 花费(USD) | 平均延迟 |
|---|---|---|---|---|---|
| google/gemini-3.6-flash | 124341 | 14322 | 0 | 0.2939 | 4162ms |
| moonshotai/kimi-k2 | 158386 | 14294 | 32896 | 0.1232 | 9866ms |

合计 $0.4171（回执账单）
开跑前估算 $0.2854（估算口径：每手 2500 输入/200 输出，每场 40 手（偏高估））
缓存：缓存有命中：稳定前缀（系统提示词）确实被复用了
