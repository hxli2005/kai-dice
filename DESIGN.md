# 《开！》设计文档 —— 生效判断

> 归档于 **2026-08-10**。本文件只装三类东西：
>
> | 类 | 是什么 | 凭什么留在这 |
> |---|---|---|
> | **规则** | 定义性选择 | 不需要证据，只需要自洽 |
> | **约束** | 架构、合规、纪律 | 做了就是真的，自我实现 |
> | **已验判断** | 有实测支撑的结论 | 逐条标注证据，见 §4 |
>
> - 未验证的假设 → **ASSUMPTIONS.md**（**不得作为施工依据**）
> - 已作废与已搁置 → **ARCHIVE.md**（**不是设计，不得据以施工**）
> - 归档前的完整版本（682 行）见 git 历史。
>
> **归档纪律**：宁愿少一条判断以后再补，也不要用没确认的假判断影响决策。
> 往本文件加东西之前先问：它是规则、是约束，还是我在赌？是赌就去 ASSUMPTIONS.md。

---

## 0. 这是什么

单机大话骰。对手是一个接入大语言模型的 AI 客户端，与玩家共用同一套引擎接口——同样的信息、同样的动作、同样的结算。它跨场记录玩家的打法，并在对局中引用。

平台：移动端优先的网页（9:16），无账号，本地存储。

> 口径注：本节只描述事实。关于"这为什么会让人上头"的一切断言在 ASSUMPTIONS.md。

---

## 1. 规则

### 1.1 基础对局（一"场"）

- 双方各 **5 颗骰子**，暗骰各自可见。
- 轮流**报数**："桌上（双方合计）至少有 N 个 X 点"。
- 报数阶梯必须抬高：数量加大，或同数量下点数加大。首报自由（数量 ≥ 2）。
- 任一方在对方报数后可以**开**（质疑）。开牌清点：
  - 报数成立 → 开的人**输**；不成立 → 报的人**输**。
  - 每局输家 **失去一颗骰子**。
- **骰子即血条**：骰子越少，私有信息越少。一方骰子归零，场终。
- **癞子（飞）**：1 点为万能牌，可被「斋」关闭。
- **阶梯边界**：飞局可报点数 2–6（癞子不可作为报点），斋局 1–6；点数自然序 1<2<…<6；数量上限 ＝ 场上总骰数，报满后下家唯一合法动作是开。
- **先手**：首局玩家先报，此后每局由上局输家先报——输家同时握有斋的宣言权（败者翻盘杠杆，有意设计）。
- **看骰是公开动作**：骰子开局盖住，对方能看到你何时揭盅。揭盅时机进入阅读通道。
- 节奏预算：一场 5–9 局，4–7 分钟；单局 30–60 秒。

### 1.2 注池与账本

- 每局开始双方各下 1 底注入池；**每次报数双方各自动追 1 注**。
- 开牌者赢池或输池。**筹码不决定胜负**（骰子决定），筹码是**战绩的分辨率**。
- 初始筹码各 100。赔率乘法叠加：盲 ×2（双方都盲 ×4）、斋 ×1.5，转移额四舍五入。
- **筹码可为负、不触发任何终局条件**——终局只由骰子决定。
- **跨场账本**：身家跨场连续、场终写回、可为负。赊账**无硬上限、永不锁玩**。「翻篇」清账随时可用且免费，但**记入档案**（"翻篇 N 次"）。
- **机房账本**：AI 为独立玩家，各有身家。**户头按型号记**——同一型号无论走托管、暗号还是自带钥匙，都是同一个账。**AI 可为负、但无翻篇**；AI 回血唯一通道＝**离屏夜场**：每自然日结算 1 场 AI 互打（回大厅触发，沉默 bot 模拟、决定性可回放），单日净转移截断 ±20。
- **身家＝净转移，全席从 0 起（Q88）**：榜上**不预置任何数字**，谁都没有发下来的本金。一场没打，榜就是空的——空榜是诚实的空，不是坏了。原先分档的初始身家（800／500／300／玩家 100）随 Q87 一并退场：那组数出自角色叙事（"三十年家底"／"街口薄底"），与性格脚本同源。
  ⚠️ **连带失效**："把它打到榜上红字"从长线目标变成第一场就可能发生的事——原来"打穿≈20–50 胜"的爬坡感没有了。这是接受项，不是疏漏；要不要另造长线目标感，等真有对局数据再说。
- 红线：筹码为虚拟分数，**永不接真钱，不可提现**。

### 1.3 三闸（一键的高危发言）

报数前可选扳一枚闸：

| 闸 | 效果 | 赔率 |
|---|---|---|
| **盲** | 未看自己骰子直接报 | 本局池 ×2（双向） |
| **斋** | 本局 1 不为癞子 | 本局池 ×1.5 |
| **抬** | 轮到你时扳闸，全桌对等生效，每人每局限一次 | 本局池 ×2 |

**窗口**：
- 斋：仅本局首报者、且在其首报前可宣——斋改变整局物理，必须在任何报价存在之前定下。
- 盲：本局**只要尚未看骰**即可宣（已报过数也行——追认既成的裸报）；宣后整局不得看骰。
- 盲、斋可叠加（首报者），赔率相乘。

**深水线（自动）**：阶梯抬至第 6 档起池 ×2——零操作的断点，全桌可见地逼近。

三闸是玩家侧唯一的"主动发言"：一次点击，语义完整，全桌可见。

### 1.4 时间

- **默认无倒计时、无超时代报**——PvE 不存在拖时间的公平问题。
- 挂机 >30 秒触发催话，纯氛围，**无机制后果**。
- **用时全程静默记录**，只进决策与档案，不进倒计时 UI。

### 1.5 三人桌（副模式；单挑为默认）

- 轮转报数，**开只能开上家**；开牌局双方结算，输家掉骰，下一局由输家先报。
- 报数上限＝场上总骰数（3×5=15 起步）；斋仍限本局首报者。
- 注池：每次报数**全桌各追 1 注**，开牌局胜者收池。
- **淘汰观战**：骰尽者出局旁观，余者续战至一人独存。
- **好友房**：链接邀请制，无大厅、无陌生人匹配。**AI 永远至少占一席**；无好友＝2AI+1 人，好友掉线＝AI 代打，好友到场＝AI 让座。
- **权威层**：多人房引擎移服务端（同一份零依赖引擎），掷骰与暗骰服务器持有，承诺哈希照发；solo 保持客户端。
- **人类之间无自由文本**：短语盘＋三闸＝全部社交动词（零 UGC 审核面）。AI 是桌上唯一自由说话者，其调用计房主配额。
- **反串谋（Q86 修正）**：信息层由架构自动保障——每个 AI 客户端只见公开事件，互相看不见骰面，**物理上无法串牌**。提示词只陈述规则事实「**无队伍，各自为战**」；原"不许联手针对第三方"的行为要求已随 Q86 删除。
  ⚠️ **残留敞口，明示接受**：它们仍可能**嘴上配合**压玩家。原 §1.5"两个 AI 围剿玩家＝感知作弊，宪法级禁止"**此后没有执行手段**——若三人桌实测出现此体验，只能靠改结构（如错开发言顺序、限制互相引用）解决，**不许退回提示词求它**。

### 1.6 术语

| 词 | 指什么 |
|---|---|
| **型号** | 对手的身份，也是卡面上的名字（事实性标注）。**跨场档案、身家户头、榜，全部按型号记** |
| **托管席** | 官方通道上那一个免费型号，零配置一键可玩 |
| **客席** | 自带钥匙（BYOK）的座位，你填什么型号它就是谁 |
| **它** | 全文与界面对对手的统一指代 |

**一张对手卡 ＝ 一个型号。**（Q89，2026-08-10）

原先的"机位"层（一号机／二号机／三号机＝同型号坐不同座位、卖明牌座位规则的差异）**整体退场**。理由：一号机与二号机钉的是同一个型号，差别只有"桌上给不给算盘"，而那是 Q51 写明的**兜底手段**（"分化不足时的最小区分"）——**证据闸门尚未跑，先用兜底等于用我们造的差异掩盖正要测的问题。**

**工具可用性的机制没有删**（`gear.calc` / `gear.usesBlind` 照常生效，有测试守着），只是名册上不再拿它捏对手。若素颜擂台的结论是"模型之间分不开"，这层可以原样启用——那才是它该出场的时候。

---

## 2. 架构宪法

> 本节为最高优先级约束，凌驾于一切实现便利。

### 2.1 引擎只认玩家接口

1. 引擎只暴露 `observe()`（自己的骰子＋公开事件流）与 `act()`（报数/开/宣言）。**引擎不知道也不关心接口对面是人还是 AI。**
2. **同构接入**：AI 客户端与人类 UI 调用同一套 API，拿到同构的字节。它看不到你的骰子，因为**接口里没有这个字段**——公平由 schema 强制，不由承诺保证。
3. **进程隔离**：AI 客户端独立运行。AI 崩溃 ≠ 游戏崩溃。
4. **引擎零后门**：不为 AI 提供任何额外查询、任何提前通知、任何重掷。审计方法：接口层抓包即可验证 AI 收到的信息集合。

结构红利：PvP-ready；AI 客户端 ×2 即自对弈；人类局与 AI 局共用同一事件流格式。

### 2.2 信任协议

- **承诺哈希**：每局开始，双方骰子（含 AI 的）以 `hash(骰面‖nonce)` 上屏；摊牌时开封验证。
- **决定性回放**：种子＋事件流可完整复算一场；回放文件可导出。
- **输入洁净**：玩家提交前的任何信号（悬停、触摸未落）不采样。

### 2.3 AI Harness

AI 客户端内部为 **agent 制**：一个 LLM agent ＋ 两类注册工具，自主决定是否调用。

| 组件 | 内容 | 公平性依据 |
|---|---|---|
| LLM agent | 局面叙事＋自主工具调用＋输出动作与台词 | 与人类同构的信息与动作 |
| 事实工具：概率计算器 | 给定自有骰与报价，算"该报价为真"的概率 | 假肢而非外挂 ＋ **双发** |
| 事实工具：档案笔记 | 对该玩家的长期观察，读写自由 | 明牌可读（§2.4） |
| ~~策略工具~~ | ~~CFR 策略表 / 最优动作推荐~~ | **禁止** |

**两条红线**：
1. **工具只许告诉它世界是什么样，不许告诉它该怎么做。**（预装均衡策略表＝不可读，会单向阉割双向读心——这是红线的存在依据。）
2. **事实工具必须双发**——任何一方拿到的事实，另一方界面里必须有等价物。

**回归门禁**：清单 bot（永远开／从不虚报／复读机／纯随机）上线前必测。agent 不需要碾压，但**不许被无脑策略稳定击穿**。

**决策日志**：每手记录工具调用序列、推理摘要、最终动作。它是"台词不许编"的事实来源，也是审计与红队素材。

**降级链**：自带 API → 官方限额通道 → **沉默模式**（纯数学行棋，不说话）。任何一层断了游戏都能打。
**沉默模式的被读底线**：开牌时必须附一段由事实模板生成的推理（零模型、全事实）——降级可以降话，不许降掉唯一的差异化。

### 2.4 档案

- **明牌档案**：玩家随时可打开"它眼中的你"全文（虚报率、开人阈值、它给你的判词草稿）。这是 §2.3 双发红线的玩家侧等价物，**不是慈善**——看过档案的你会试图反装，而反装也是它的阅读材料。
- 档案**按型号记**（与身家户头同口径）。
- **阅读通道**（它记录什么）：① 报数序列；② 开人模式（质疑阈值、池深下的怯懦点）；③ 条件倾向（连败后虚报率变化、大池下开牌阈值移动、被嘲讽后的下一手）；④ 用时指纹；⑤ 三闸习惯；⑥ 以上全部随局数累积。
- **高倍池加权**：高倍率局入档案与判词的记忆加权（×8 的池必须被记住）。
- **复盘合宪性**：本作每局必以开牌收尾，开牌即双方骰面公开，故局终 AI 查看你的骰面与选择序列使用的**全部是已公开信息**，不触碰 §2.1 信息隔离（对局中依然看不见）。

---

## 3. 事实与言论纪律

> 总纲（**场合律**）：**系统说话零容忍，它说话零审查。**

**系统的场合**（结算数字／报告卡数据面／档案客观层／表盘／规则事件）＝引擎盖章，**错即 bug**。

**它的场合**（台词／判词意见话／留档）＝解链：误判、过度自信、言行不一、记仇、偏见、把旧事记歪，**全部放行**——那是野生对手的活性，不是故障。

**三锁不放**（任何身份不豁免）：
1. **信息边界公平**（§2.1）；
2. **动作合法、引擎结算**（§2.1）；
3. **真迹不可赛后重写**——**禁事后供词**：解释"为什么"只许来自当时的留档，不许散场后追问模型动机（事后合理化＝制造回忆）。

**其余生效条款**：

| 条款 | 内容 |
|---|---|
| **台词双层制** | **裁判层永不说谎**（报数记录、结算、复盘、关于你战绩的嘲讽——一字不许编）；**牌手层允许诈**（AI 关于自己手牌与意图的发言可以虚张、误导、演戏） |
| **一句话边界** | **可以骗玩家它怎么想，不能骗玩家发生过什么。** |
| **事件接地** | **语义可错，指代不可错。** `{actor, target, action, round}` 四元组是世界的骨架，不属于任何一方的"说法"。下游一律从四元组读事实，**禁以自然语言复述为事实来源**。判据：偶尔记错数字＝人格；反复把"谁开谁"弄反＝数据接地故障 |
| **事实转写禁令** | 模型写意见，引擎写事实。公共事实经槽位回填或对账校验，任何层不许错 |
| **诈必须留档** | 决策日志带 `belief` ＋ `speechMode:bait` 私密字段——对外可演，对内交底。**无留档意图的事实错＝故障，不许包装** |
| **蒙报类目** | 未看骰的报价单独归类；"老实人"类判定需以看过骰为前提；宣言盲与事实未看分开跟踪 |
| **禁报假精度** | 遥测不许以仪表盘读数出口（"你磨了 12.3 秒"），必须译成现象学语言（"你手停了一下"）。读与说解耦：分析层用全部通道，表达层降级遥测。**由数据管线强制，不靠提示词求它**——原始毫秒在进提示词之前就已转成现象语言，模型拿不到那个数 |
| **算过才能引数** | 未公开算过，不得引用精确概率（粗略心算自由）。随口报数不禁止，但须在留档留痕——**不管住嘴，管住账** |
| **提示词全席同构** | 名册上每个型号与擂台席共用同一份系统提示词，**无任何分支**（Q89 后连名字标签都没有了）。人为差异一律不许藏在提示词里 |
| **提示词二准入**（Q86） | 提示词（system 与 user **两段同规**）只准装两类东西：**① 规则与操作**——系统怎么运作、能做什么动作、怎么输出；**② 数据**——这局实际发生了什么。**其余一律删**：解释、提醒、鼓励、许可、节拍要求、风格约束、策略暗示，以及"做你自己""没有派给你的性格"这类**元指令**（它仍然在规定它该是谁）。判据一句话：**怎么选，是它的判断，不是我们的输入。** |
| **约束长在管线上** | 需要保证的东西写进代码，不写成对模型的请求。已按此办：秒数在进提示词前转成现象语言；台词长度交给 `max_tokens`；嘴手是否一致交给渲染层（UI 本就单独显示报价）；串牌由架构的信息隔离物理阻断 |
| **全席逐字相同** | 系统提示词**不含名字标签**——`narrate()` 对自己一律返回"你"，模型不需要知道自己叫什么；名字是给玩家看的脸，不是给模型的数据。名册上每个型号与擂台席拿到的 system **完全逐字相同，无任何分支** |
| **反脚本红线** | 禁止把三闸触发写成确定性规则（EV 阈值脚本）——扣扳机的必须是模型，否则时机可被逆向成查表 |
| **禁错误注入** | 不许用失误率调难度。难度调的是学习率与风格，永远不是失误率 |
| ~~内容底线~~ | **2026-08-10（Q85）取消。** 我方不再向模型下发任何内容约束，出口侧也无过滤——台词的内容边界完全交给模型自带的安全对齐。合规姿态见 §5.4 |
| **台词不阻塞** | 台词随动作流式输出，永不阻塞玩家输入 |
| **开牌须念理由** | 开牌时必须念出理由，且理由由真实决策数据生成 |
| **AI 生成标识** | AI 台词挂"AI 生成"标识 |

---

## 4. 已验判断（唯一有实测支撑的一节）

> 证据编号见 ASSUMPTIONS.md 的证据台账。**全部实证总量是七笔**——这一节的短，是诚实，不是遗漏。

| # | 判断 | 证据 |
|---|---|---|
| **C1** | 常驻精确概率表盘会把玩家退化为**阈值执行器**（低概率直接开、高概率不敢开，减少思考）。→ **「算」改为主动公开动作**：被动零显示、按钮百分比删除、算的动作全桌可见而结果仅算者私有；全游戏无精确百分比；AI 同规则（预注入精确概率退役）。算频成为原生 tell | **E3＋E5＋设计预言，三源合流。本文件最硬的一条** |
| **C2** | 记错事实的读心者摧毁核心幻想——信任不对称：读得越像，错一次伤越重 | E6（上线三 bug） |
| **C3** | 接地故障真实存在（反复把"谁开谁"弄反），且它与"人格化的记错"必须分开处理 | E4 |
| **C4** | 报告归属错位＝裁判层说谎＝P0 违宪。多人报告须按型号归属拆分，参战/全桌局数拆行 | E3 |
| **C5** | "观察特征 → 形成判断 → 影响动作 ＋ 跨场引用"这条链**能跑通** | E4，**n=1，26 局才长出来** |
| **C6** | 单人净桌对硬核玩家的深度天花板 ≈ 两天 | E5，n=1（作者） |
| **C7** | 单场成本量级：Haiku ≈¥1／Sonnet ≈¥2／Opus ≈¥5。**一轮 8 模型 ×10 场的擂台（≈280 场）会穿月红线 ¥500** | E7 |
| **C8** | 沉默模式下无开场白 ⇒ 无通道的玩家永远看不到跨场记忆 | E3 反推；处置见 SYNC 待决 Q50 |

**降本三件（与 C7 配套，已验量级）**：① prompt caching——全席提示词逐字相同即理想缓存形态，稳定前缀打断点。**坑：可缓存最小前缀分档（Haiku 4.5 需 4096），前缀不够长时缓存静默失效，必须用返回的 `cache_read_input_tokens` 验证非零**；② `max_tokens` 收紧；③ 离屏跑批用 Batch API 五折。

---

## 5. 工程与合规约束

### 5.1 范围纪律
- web、移动优先 9:16、无账号（localStorage 档案）、本地存储。
- **唯一豁免**：多人好友房引入服务端权威与房间链接，但仍无账号（设备 ID 即身份）、无大厅、无陌生人撮合。
- 账号是功能的代价不是门票：首局永远零门槛。

### 5.2 模型接入
- **双通道**：默认官方通道（内置低成本模型，每日限额）；设置页可填自己的 API——`base_url + api_key + model`，兼容 OpenAI 格式＋Anthropic 格式。
- **OpenRouter**：**BYOK 专属，官方通道绝不开放**。模型清单动态拉取（禁硬编会腐烂的名单）；按其惯例带来源标识头；**实验用途须锁定供应商路由**（同一模型 ID 会被路由到不同后端/量化版本）。
- **钥匙不出设备**：key 只存 localStorage，浏览器直连模型服务商，我方服务器永不经手。
- **官方通道必须走服务端代理**（我方 key 不落客户端），代理只收结构化局面、只回合法动作与台词。配额按设备计，超额降级沉默模式，BYOK 无限。
- **禁止把消费级订阅桥接为 API 后端**——违反服务条款、封号风险，且与 BYOK 架构不兼容。

### 5.3 成本
- 成本恒等式：**上限 ＝ 配额 × 模型单价**，硬封顶。超配额不挡玩，只闭嘴。
- **全局熔断器**：代理侧设全网日 token 预算（独立于按设备配额），烧完当天全体降级沉默模式。
- **月度预算红线 ¥500**，上调需显式决策。
- 台词分级省调用：例行动作用模板＋变量填充，戏眼时刻才烧 token。
- 基建：静态托管（Cloudflare Pages）＋边缘函数代理，固定成本≈0。

### 5.4 保命三律

**法律线**
- **筹码三不：不可充值、不可提现、不可转移。** 任何变现永不接触筹码经济。
- 国内探针期**零商业化**；收钱先海外。
- 应用内零 UGC 自由文本（玩家侧内容风险面在设计层删除）。
- **AI 台词内容：我方零约束、零过滤（Q85，2026-08-10）。** 提示词不含内容条款，出口侧无过滤器——边界完全依赖模型自带的安全对齐。**明示接受项**：① 商业模型各有对齐层，这是实际起作用的那道闸；② **无对齐的本地模型（Ollama 等 BYOK 路径）没有任何闸**，其输出由玩家自己负责；③ 官方通道所用型号的对齐水平，成为一项须实测的选型指标。任何面向玩家的文案不得再声称我方设有内容底线。
- 手续排期：ICP 备案 → 软著 → 商标 → **第一笔收入前：注册经营主体＋游戏合规律师咨询**（触发条件写死）。

**抄袭线**
- 玩法与规则不受保护，不设防；受保护的是台词、代码、名称与视觉识别。
- **角色资产归零**：删人格、删人名之后本作**不再有可注册的角色 IP**，商标标的物缩为名称《开！》＋视觉识别。这是切干净的实价，不粉饰。替代护城河＝**它记着的那本关于你的账**（档案数据复利）＋迭代速度。
- 打假只走平台侵权投诉，不打官司。

**财务线**
- 全局熔断器＋月红线；免费层基建；永不买量。
- 付费动作触发条件制：公司注册等第一笔收入，付费基建等免费层实测撑不住。

### 5.5 法务红线（模型名）
- 模型名仅作**事实性标注**（指称性使用）。
- **绝不复刻官方吉祥物 / logo / 品牌视觉**（画进本作角色＝侵权＋暗示背书双重风险）；形象用本作原创美术语言。
- 页面明示"**非官方 · 无关联**"。
- 上线前查各家 API 条款对博弈类应用的标识要求。
- **命名与视觉远离赌场皮肤**；产品名、页面文案、商店描述不出现"赌场"二字。
- 擂台榜**必须带样本数**，口径限定"在这张桌子上"，**禁通用 benchmark 式断言**。

### 5.6 开源边界
- **开源公平，闭源手艺。** MIT 开源：规则引擎、玩家接口、承诺哈希与回放验证器——公平主张全部可审计。永久闭源：台词管线、行为档案 schema、官方代理与其配额/熔断策略。（提示词本身已薄到只剩规则，不再构成手艺。）
- **时序**：验证器与协议规范随发布即开；完整引擎推迟到病毒窗口期后——不在最热时给克隆者递脚手架。
- 诚实的接受项：BYOK 直连路径下提示词可被 devtools 扒走（key 不出设备与 prompt 保密不可兼得，取前者）。不做反调试军备竞赛。

### 5.7 静态发行防线
- 自有域名第一天就上；报告卡与切片内嵌域名水印＋二维码。
- **代理持魂**：官方通道按 Origin 锁域＋设备配额——镜像站搬得走静态壳，搬不走嘴。
- 托管用 Cloudflare Pages（支持私有仓库部署；GitHub Pages 免费档强制公开仓库）。

### 5.8 本地化
只有两件事：**界面文案重写**（不是机翻）＋**提示词换语种**。台词由模型原生生成。
风险随之全押模型：**须按语种各跑一次素颜测试**。

---

## 6. 工作纪律

1. **红队条款**：重大裁决必附「看空段」（结构化反方，不许一句话敷衍）。
2. **蓝图冻结**：v3+ 蓝图区停止扩张——已经够厚，地基未验。
3. **验证闸门**：下一轮设计投入前，先取 **≥10 名陌生外部玩家、每人 ≥3 场**的真实数据（次日回访率、报告卡回流数、流失访谈）。阈值草案：回访 <20% 且零自发传播 → 定位降档为"精致玩具＋方法论名片"；回访 ≥40% 或出现自发传播 → 产品路线继续；中间带 → 扩样再测。
4. **叙事戒律**：禁"全闭环／宿命／图纸没变"式修辞——叙事美感不是证据；引用先例须同时报存活者偏差。
5. **三文件纪律**（本次归档新增）：施工依据只有 DESIGN.md；ASSUMPTIONS.md 的条目引用时必须带证据状态；ARCHIVE.md 的内容不得据以施工。**往 DESIGN 加东西前先分类：规则 / 约束 / 赌注。**

---

## 附. 参数表（默认值）

| 参数 | 默认 | 备注 |
|---|---|---|
| 起始骰数 | 5 | |
| 首报下限 | 2 个 | |
| 盲 / 斋赔率 | ×2 / ×1.5 | |
| 抬倍率 / 限次 | ×2，每人每局 1 次 | |
| 深水线 | 第 6 档起 ×2 | |
| 每手追注 | 1 | 池深曲线的斜率 |
| 虚报判定线 | P(真\|自见骰) < 50% | ⚠️ 与「蒙报类目」的边界未写清：未看骰时"自见骰"指什么，待补 |
| 初始身家（全席，含玩家） | **0** | Q88：身家＝净转移，榜上不预置数字；户头按型号记 |
| 局内起始筹码 | 100 | 单场内的池与净胜口径——与跨场身家是两码事 |
| 夜场 | 每自然日 1 场，单日净转移截断 ±20 | ⚠️ 若擂台放量跑，此截断的设计意图（玩家为身家主要变量）会失效，见 ARCHIVE 附录 |

> 已移除的参数行及其原因见 ARCHIVE.md（软计时 20s、AI 每手调用成本 0.5 元/场、人格映射阈值、雪球与触债率目标带）。
