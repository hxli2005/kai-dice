# 项目：《开！》——单机大话骰，AI 当对手

单机大话骰：对面是一个会算概率、会记你习惯、开牌前先亮出想法的 AI。
生效判断见 [DESIGN.md](DESIGN.md)（设计宪法，最高权威）。

## 双 Session 协作协议

本项目由两个并行 session 协作，通过文件通信：

- **设计 session**：只讨论游戏取舍，产出必须落盘。**唯一**有权修改 DESIGN.md 与本文件。
- **编码 session**：负责实现与测试。DESIGN.md 对其只读。

**角色识别**：用户让你写代码、跑测试、调试 → 你是编码 session；用户在聊设计、取舍、玩法 → 你是设计 session。按对应守则行事。

## 文件契约

| 文件 | 作用 | 写者 | 对施工的效力 |
|---|---|---|---|
| DESIGN.md | **生效判断**：规则／约束／已验判断 | 仅设计 session | **唯一施工依据** |
| ASSUMPTIONS.md | 未验假设＋证据台账 | 仅设计 session | **不得作为施工依据**；引用时必须连证据状态一起引用 |
| ARCHIVE.md | 已作废（僵尸条款）与已搁置（冰箱） | 仅设计 session | **不是设计，不得据以施工** |
| SYNC.md | 信箱：待决问题 ↑ / 已决裁决 ↓ / 变更日志（**只留现阶段**） | 双方（各写各区） | 待决区的占位实现须带 `TODO(Qn)` |
| SYNC-ARCHIVE.md | 信箱冷库：已执行完的裁决全文／已裁待决原文／已完工施工单／旧变更日志 | 双方（只追加归档批） | **仅史料，不得据以施工** |
| CLAUDE.md | 本协议 | 仅设计 session | — |

> **三文件分家于 2026-08-10**（SYNC 已决 Q84）。原因：DESIGN.md 里混着大量未验证的市场与体验断言，
> 却以宪法语气影响施工取舍。分家原则——**规则 / 约束 / 赌注，三者分居**。
> 往 DESIGN 加东西之前先分类；分不出来的，默认进 ASSUMPTIONS.md。
> 表现层（UI）判断已整体归档，**DESIGN 不再对界面做任何规定**。

## 编码 session 守则

0. 若无 git 仓库：先 `git init` 并建 .gitignore。小步提交；DESIGN/SYNC 的变更单独提交（diff 即变更历史）。
1. 开工先读：DESIGN.md 全文 + SYNC.md。**ASSUMPTIONS.md、ARCHIVE.md 与 SYNC-ARCHIVE.md 不必读，也不许照做**——分别是赌注、坟场与史料，都不是图纸。
2. **设计缺口三分法**（核心纪律）：
   - DESIGN.md 已覆盖 → 照做；
   - 纯实现细节（玩家不可感）→ 自行决定，SYNC 变更日志记一行；
   - **影响玩家可感体验**（规则、数值、节奏、公平、文案口径）→ 写入 SYNC「待决问题」（编号 Qn），用占位实现 + `TODO(Qn)` 标记，**继续做别的，不停工、不发明设计**。
   - ⚠️ 归档后新增：**在 ARCHIVE.md 里找到的东西不算"DESIGN 已覆盖"**。想用就先走待决区，由设计 session 捞回。
3. 不可协商项：DESIGN §2 架构宪法（引擎只暴露玩家接口，AI 是外接客户端，零后门）；§2.3 工具红线（事实工具双发，策略工具禁止）；§3 事实与言论纪律。
4. 技术选型自决，但须满足 §5.1 范围纪律（web、移动优先 9:16、无账号、localStorage）。
5. 每完成一个可运行的里程碑，SYNC 变更日志记一行（设计 session 靠它了解进度）。
6. **DESIGN 不再规定界面**。UI 取舍归编码 session 自决，只需守两条硬约束：§3 的"台词不阻塞输入"，与 §5.5 的"命名与视觉远离赌场皮肤"。

## 设计 session 守则

1. 每次讨论开始，先读 SYNC「待决问题」，优先裁决积压项。
2. **口头结论必须落盘**：裁决写入 SYNC「已决」（引用 Qn），持久规则合并进 DESIGN.md，大改在变更日志记一行。不落盘的讨论视为没发生。
3. 不写代码，不指挥实现细节——只定义"玩家可感"层。
4. **落盘前先分类**（2026-08-10 新增）：一条结论是**规则**（定义性，只需自洽）、**约束**（做了就是真的）、还是**赌注**（关于玩家/市场会怎样的断言）？前两者进 DESIGN，第三者进 ASSUMPTIONS 并**必须写出证伪条件**——写不出证伪条件的不是假设，是修辞，不许落盘。
5. **红队条款**：重大裁决必附「看空段」（DESIGN §6.1）。
6. **归档不是删除**：作废与搁置的设计移入 ARCHIVE.md 并注明死因，不从版本库里抹掉。
